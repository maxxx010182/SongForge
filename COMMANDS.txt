SongForge — КОМАНДЫ ДЛЯ СЕРВЕРА (копируй ТОЛЬКО одну строку за раз)
Версия: 2.11.26 — жанр из идеи (реп≠поп); AI-продюсер без дефолт Поп; enforce style

=== АКТУАЛЬНОЕ (15.07 — деплой v2.11.26: жанр/реп) ===

# Что в 2.11.26:
# - «Жанр реп» в тексте важнее дефолтного Поп с UI
# - AI-продюсер не шлёт selectedGenre=Поп
# - unified style: enforce hip-hop, срез Modern Pop leak
# - бек-вокал / подпевки лучше парсятся
# - модель V5_5; lyrics ~4 мин (2.11.25)

ssh root@195.19.20.245

cd ~/SongForge

curl -fsSL "https://raw.githubusercontent.com/maxxx010182/SongForge/main/scripts/update-now.sh?$(date +%s)" | bash

# Ждать: === Готово! v2.11.26 ===

curl -s https://sozdaipesnu.ru/api/health
# "version":"2.11.26"

# Тест: AI-продюсер + «Жанр реп, стиль Баста…» → style должен содержать hip-hop/rap, не Modern Pop


=== Ноты не пришли ===
# docs/instrukcii/GETPLATINUM-NOTY-RUNBOOK.txt

sqlite3 data/songforge.db "SELECT id, user_id, status, notes_amount FROM payment_orders ORDER BY created_at DESC LIMIT 5;"

pm2 logs songforge --lines 40 --nostream | grep -i -E 'getplatinum|mark_paid|verify'


=== ОБЫЧНОЕ ===
pm2 status
pm2 restart songforge --update-env
