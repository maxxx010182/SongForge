SongForge — КОМАНДЫ ДЛЯ СЕРВЕРА (копируй ТОЛЬКО одну строку за раз)
Версия: 2.11.55 — бот MAX, нужен деплой

=== АКТУАЛЬНОЕ (GitHub закрыт = 404) ===
# Путь без GitHub: архив с рабочего стола. Инструкция: docs/instrukcii/DEPLOY-LOCAL.txt
# Архив: C:\Users\makc0\Desktop\songforge-update.tar.gz
# Залить в /tmp/songforge-update.tar.gz (WinSCP или scp).

cd ~/SongForge

tar -xzf /tmp/songforge-update.tar.gz scripts/deploy-local.sh

bash scripts/deploy-local.sh

# Скрипт сам перезапускает ДВА процесса: songforge и worker.
# Дождаться: === Готово! v2.11.55 ===

curl -s https://sozdaipesnu.ru/api/health
# "version":"2.11.55"
# "max_bot": true

# Если скрипт ругнулся на версию — файлы уже скопированы, тогда:

pm2 restart songforge --update-env

pm2 restart songforge-worker --update-env


=== ЕСЛИ GITHUB ОТКРЫТ (как раньше) ===
cd ~/SongForge

curl -fsSL "https://raw.githubusercontent.com/maxxx010182/SongForge/main/scripts/update-now.sh?$(date +%s)" | bash

# Если версия старая — эту же длинную строку ещё раз.


=== ОБЫЧНОЕ ===
pm2 status
pm2 restart songforge --update-env
pm2 restart songforge-worker --update-env
