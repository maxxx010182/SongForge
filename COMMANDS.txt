SongForge — КОМАНДЫ ДЛЯ СЕРВЕРА (копируй ТОЛЬКО одну строку за раз)
Версия: 2.11.55 — бот MAX, нужен деплой

=== АКТУАЛЬНОЕ ===
# Как всегда. Сначала длинная строка.
# Скрипт сам поднимает songforge и worker; если версия старая — эту же строку ещё раз.

cd ~/SongForge

curl -fsSL "https://raw.githubusercontent.com/maxxx010182/SongForge/main/scripts/update-now.sh?$(date +%s)" | bash

# Если эта строка дала 404 — запасной путь (DEPLOY-SEYCHAS):

curl -fsSL "https://codeload.github.com/maxxx010182/SongForge/tar.gz/main" -o /tmp/sf.tar.gz

tar -xzf /tmp/sf.tar.gz -C /tmp SongForge-main/scripts/deploy-vps.sh

cp /tmp/SongForge-main/scripts/deploy-vps.sh scripts/deploy-vps.sh

bash scripts/deploy-vps.sh

# Дождаться: === Готово! v2.11.55 ===
# Если версия старая — длинную curl-строку (или bash scripts/deploy-vps.sh) ещё раз.

curl -s https://sozdaipesnu.ru/api/health
# "version":"2.11.55"
# "max_bot": true

# Потом в MAX: https://max.ru/id720401732483_bot  → «Начать»


=== ОБЫЧНОЕ ===
pm2 status
pm2 restart songforge --update-env
pm2 restart songforge-worker --update-env
