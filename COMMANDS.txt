SongForge — КОМАНДЫ ДЛЯ СЕРВЕРА (копируй ТОЛЬКО одну строку за раз)
Версия: 2.11.38 — превью TG: карточка /t/ID/og.jpg 1200x630

=== АКТУАЛЬНОЕ (v2.11.38) ===

ssh root@195.19.20.245

cd ~/SongForge

curl -fsSL "https://raw.githubusercontent.com/maxxx010182/SongForge/main/scripts/update-now.sh?$(date +%s)" | bash

# если Pillow не встал сам — один раз:
# source .venv/bin/activate 2>/dev/null; pip install -r requirements.txt; pm2 restart songforge --update-env

curl -s https://sozdaipesnu.ru/api/health
# "version":"2.11.38"

# Проверка превью-картинки (ID из вашей ссылки /t/ID):
# curl -sI https://sozdaipesnu.ru/t/ID/og.jpg
# в браузере: https://sozdaipesnu.ru/t/ID/og.jpg  — должна открыться широкая карточка

# Telegram кеширует старое! Обязательно:
# 1) @WebpageBot → отправить https://sozdaipesnu.ru/t/ID
# 2) вставить ссылку В НОВОЕ сообщение (не старое)


=== ОБЫЧНОЕ ===
pm2 status
pm2 restart songforge --update-env
