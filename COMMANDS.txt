SongForge — КОМАНДЫ ДЛЯ СЕРВЕРА (копируй ТОЛЬКО одну строку за раз)
Версия: 2.11.84 в GitHub. Нижний бар + ленд podarok.

=== АКТУАЛЬНОЕ ===
# 1) код студии (бар 2.11.83) + файлы ленда
curl -fsSL "https://raw.githubusercontent.com/maxxx010182/SongForge/main/scripts/update-now.sh?$(date +%s)" | bash

curl -s https://sozdaipesnu.ru/api/health
# "version":"2.11.84"

# 2) первый раз ленд на поддомен (nginx + замочек). Повторно — тоже безопасно.
bash /root/SongForge/scripts/install-podarok.sh

# 3) проверка ленда (после DNS, иногда 15–60 мин)
curl -sI https://podarok.sozdaipesnu.ru | head -5


=== ОБЫЧНОЕ ===
ssh root@195.19.20.245
pm2 status
pm2 restart songforge --update-env
pm2 restart songforge-worker --update-env

curl -fsSL "https://raw.githubusercontent.com/maxxx010182/SongForge/main/scripts/update-now.sh?$(date +%s)" | bash
