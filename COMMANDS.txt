SongForge — КОМАНДЫ ДЛЯ СЕРВЕРА (копируй ТОЛЬКО одну строку за раз)
Версия: 2.11.29 — бета-лимиты мягче: concurrent 6/2, trial 8 IP/сутки

=== АКТУАЛЬНОЕ (15.07 — деплой v2.11.29) ===

# Лимиты (env можно переопределить):
# MAX_CONCURRENT_GENERATIONS=6  — одновременно «варятся» на всём сервере
# MAX_CONCURRENT_PER_USER=2     — одновременно на одного user
# MAX_TRIAL_PER_IP_PER_DAY=8    — пробных с одного IP за день
# (1 пробная на аккаунт — без изменений)

ssh root@195.19.20.245

cd ~/SongForge

curl -fsSL "https://raw.githubusercontent.com/maxxx010182/SongForge/main/scripts/update-now.sh?$(date +%s)" | bash

# Ждать: === Готово! v2.11.29 ===

curl -s https://sozdaipesnu.ru/api/health
# "version":"2.11.29"

# Бэкап / health (если ещё не ставил cron — см. ниже)
chmod +x scripts/backup-db.sh scripts/health-watch.sh
./scripts/backup-db.sh

# crontab -e:
# 15 3 * * * /root/SongForge/scripts/backup-db.sh >> /var/log/songforge-backup.log 2>&1
# */5 * * * * /root/SongForge/scripts/health-watch.sh >> /var/log/songforge-health.log 2>&1


=== ОБЫЧНОЕ ===
pm2 status
pm2 restart songforge --update-env
