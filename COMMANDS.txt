SongForge — КОМАНДЫ ДЛЯ СЕРВЕРА (копируй ТОЛЬКО одну строку за раз)
Версия: 2.11.55 — бот MAX, нужен деплой

=== АКТУАЛЬНОЕ ===
# GitHub снова открыт. Как раньше: одна длинная строка.
# Скрипт сам поднимает songforge и worker; если версия старая — запускает ещё раз.

cd ~/SongForge

curl -fsSL "https://raw.githubusercontent.com/maxxx010182/SongForge/main/scripts/update-now.sh?$(date +%s)" | bash

# Дождаться: === Готово! v2.11.55 ===
# Если версия старая — эту же длинную строку ещё раз.

curl -s https://sozdaipesnu.ru/api/health
# "version":"2.11.55"
# "max_bot": true

# Потом в MAX: https://max.ru/id720401732483_bot  → «Начать»


=== ОБЫЧНОЕ ===
pm2 status
pm2 restart songforge --update-env
pm2 restart songforge-worker --update-env
