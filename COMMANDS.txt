SongForge — КОМАНДЫ ДЛЯ СЕРВЕРА (копируй ТОЛЬКО одну строку за раз)
Версия: 2.11.35 — логотип в шапке + иконка закладок

=== АКТУАЛЬНОЕ (v2.11.35) ===

ssh root@195.19.20.245

cd ~/SongForge

curl -fsSL "https://raw.githubusercontent.com/maxxx010182/SongForge/main/scripts/update-now.sh?$(date +%s)" | bash

curl -s https://sozdaipesnu.ru/api/health
# "version":"2.11.35"

# После деплоя: откройте сайт, Ctrl+F5 (сброс кеша).
# В шапке — знак сердце+нота рядом с «СоздайСвоюПесню».
# Во вкладке браузера — та же иконка.
# Смена темы в профиле — цвет знака меняется вместе с текстом.


=== ОБЫЧНОЕ ===
pm2 status
pm2 restart songforge --update-env
