SongForge — КОМАНДЫ ДЛЯ СЕРВЕРА (копируй ТОЛЬКО одну строку за раз)
Версия: 2.11.37 — превью ссылки в Telegram (OG + cover proxy)

=== АКТУАЛЬНОЕ (v2.11.37) ===

ssh root@195.19.20.245

cd ~/SongForge

curl -fsSL "https://raw.githubusercontent.com/maxxx010182/SongForge/main/scripts/update-now.sh?$(date +%s)" | bash

curl -s https://sozdaipesnu.ru/api/health
# "version":"2.11.37"

# Проверка превью в Telegram:
# 1) update-now
# 2) Откройте в браузере: https://sozdaipesnu.ru/api/explore/ID/cover
#    (ID из ссылки /t/ID) — должна открыться картинка
# 3) В Telegram: @WebpageBot → отправьте ссылку /t/ID → «Update preview»
#    или вставьте ссылку в Saved Messages заново (после сброса кеша)
# 4) Должна появиться карточка с обложкой и названием


=== ОБЫЧНОЕ ===
pm2 status
pm2 restart songforge --update-env
