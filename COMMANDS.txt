SongForge — КОМАНДЫ ДЛЯ СЕРВЕРА (копируй ТОЛЬКО одну строку за раз)
Версия: 2.11.55 — бот MAX, нужен деплой

=== АКТУАЛЬНОЕ ===
# Репозиторий закрытый: raw.githubusercontent.com даёт 404.
# Скрипт уже на сервере — запускай его:

bash ~/SongForge/scripts/update-now.sh

# Если и он 404 (скачать архив с GitHub не вышло):

cd ~/SongForge

git pull origin main

pm2 restart songforge --update-env

curl -s https://sozdaipesnu.ru/api/health
# "version":"2.11.55"
# "max_bot": true

# Потом в MAX: открыть бота и нажать «Начать»
# https://max.ru/id720401732483_bot


=== ОБЫЧНОЕ ===
pm2 status
pm2 restart songforge --update-env
