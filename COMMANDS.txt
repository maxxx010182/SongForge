SongForge -- SERVER COMMANDS (copy ONE line at a time)
Version: 2.11.88 in GitHub. Full mobile adaptation and responsive overhaul for landing page.

=== 1) UPDATE CODE ON VPS ===
curl -fsSL "https://raw.githubusercontent.com/maxxx010182/SongForge/main/scripts/update-now.sh?$(date +%s)" | bash

curl -s https://sozdaipesnu.ru/api/health
# expected: "version":"2.11.88"

=== 2) LISTEN TO RECENT TRACKS DIRECTLY VIA SSH ===
sqlite3 /root/SongForge/data/songforge.db "SELECT created_at, title, music_url_a, music_url_b FROM generations WHERE status='success' ORDER BY created_at DESC LIMIT 5;"

=== 3) CHECK MAX BOT STATUS AND WORKER ===
pm2 status

pm2 logs songforge-worker --lines 40 --nostream

sqlite3 /root/SongForge/data/songforge.db "SELECT id, user_id, max_user_id, funnel_stage, nudge_step, next_nudge_at, messages_ok FROM messenger_contacts ORDER BY created_at DESC LIMIT 5;"

sqlite3 /root/SongForge/data/songforge.db "SELECT * FROM messenger_followups ORDER BY created_at DESC LIMIT 5;"

=== OTHER ===
ssh root@195.19.20.245
pm2 restart songforge --update-env
pm2 restart songforge-worker --update-env
bash /root/SongForge/scripts/install-podarok.sh
