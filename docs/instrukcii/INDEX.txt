SongForge — оглавление инструкций
==================================
Папка: docs/instrukcii/

КОМАНДЫ СЕРВЕРА (копировать по одной строке):
  COMMANDS.txt  — в корне проекта, латиница (SSH, деплой, pm2)

СЕЙЧАС АКТУАЛЬНО (июль 2026)
----------------------------
  DEPLOY-SEYCHAS.txt           — деплой если update-now падает
  GETPLATINUM-ENV.txt          — ключи оплаты в .env на VPS
  GOOGLE-POISK.txt             — почему нет в Google, Search Console
  ПРОВЕРКА-ПОСЛЕ-ДЕПЛОЯ.txt    — чеклист после обновления сайта

ПОЧТА И ВХОД
------------
  SMTP-DIAGNOSTIKA.txt         — если код не уходит на email
  REG-RU-DNS-ЗАПИСИ.txt        — MX, SPF, DKIM, DMARC + SMTP в .env
  REG-RU-ПОЧТА-ISPmanager.txt  — настройка ящика noreply@ на sm42
  SUPPORT-EMAIL-SEYCHAS.txt    — ящик support@ для поддержки (СЕЙЧАС)
  ВХОД-ПОЧТА-ПО-ШАГАМ.txt      — вход по email по шагам
  ВХОД-EMAIL-TG-VK.txt         — Telegram и VK (когда понадобится)
  VPS-SMTP-ЗАПРОС-В-ПОДДЕРЖКУ.txt — шаблон тикета REG.RU (архив)

ОПЛАТА
------
  GETPLATINUM-PREFIX-SEYCHAS.txt — prefix в .env + тест (СЕЙЧАС)
  GETPLATINUM-ENV.txt
  ВАЖНО при подключении к платежной системе.txt

ХРАНИЛИЩЕ S3
------------
  S3-REG-RU-КАК-ЗАПОЛНИТЬ.txt
  S3-ПРОВЕРКА-И-ПЕРЕНОС.txt

АДМИНКА
-------
  ADMIN-VHOD.txt               — /admin, bootstrap email

ПЛАН И РЕФЕРЕНС
---------------
  ЗАПУСК-ПЛАН.txt               — план запуска и бета
  ВИДЕО-ПРЕЗЕНТАЦИЯ.txt
  Инструкция.txt               — общая инструкция проекта
  Руководство по интеграции апи суно.txt

КОНТЕКСТ ДЛЯ АССИСТЕНТА (не инструкции)
---------------------------------------
  SONGFORGE-КОНТЕКСТ.txt       — корень проекта
  docs/NOTES_NEXT.md           — технический backlog

БИЗНЕС-ДОКУМЕНТЫ
-----------------
  docs/biznes/                 — оферта PDF, план продвижения