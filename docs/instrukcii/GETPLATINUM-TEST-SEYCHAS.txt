SongForge — GetPlatinum: деплой + тест (СЕЙЧАС)
=================================================
Файл: docs/instrukcii/GETPLATINUM-TEST-SEYCHAS.txt
Копировать команды ОТСЮДА (Блокнот), по одной строке.


ШАГ A. Подключиться (если ещё не на сервере)
---------------------------------------------

ssh root@195.19.20.245


ШАГ B. Обновить код (нет test_getplatinum.py — нужен деплой)
-------------------------------------------------------------

cd ~/SongForge

curl -fsSL "https://codeload.github.com/maxxx010182/SongForge/tar.gz/main" -o /tmp/sf.tar.gz

tar -xzf /tmp/sf.tar.gz -C /tmp SongForge-main/scripts/deploy-vps.sh

cp /tmp/SongForge-main/scripts/deploy-vps.sh scripts/deploy-vps.sh

bash scripts/deploy-vps.sh

Дождаться: === Gotovo! v2.11.4 ===


ШАГ C. Тест GetPlatinum
-----------------------

cd ~/SongForge

./venv/bin/python scripts/test_getplatinum.py


ЧТО ПРИСЛАТЬ
------------

Весь вывод шага C (ключ можно заменить на ***).

OK  = formUrl есть.
FAIL = текст ошибки целиком.