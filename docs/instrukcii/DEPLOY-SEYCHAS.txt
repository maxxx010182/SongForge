SongForge — деплой СЕЙЧАС (если update-now не работает)
======================================================
Файл: docs/instrukcii/DEPLOY-SEYCHAS.txt

Два запасных пути.

1) GitHub ещё открыт — архив с GitHub (ниже).
2) GitHub даёт 404 — с рабочего стола: DEPLOY-LOCAL.txt


--- GitHub (если curl не 404) ---

ssh root@195.19.20.245

cd ~/SongForge

curl -fsSL "https://codeload.github.com/maxxx010182/SongForge/tar.gz/main" -o /tmp/sf.tar.gz

tar -xzf /tmp/sf.tar.gz -C /tmp SongForge-main/scripts/deploy-vps.sh

cp /tmp/SongForge-main/scripts/deploy-vps.sh scripts/deploy-vps.sh

bash scripts/deploy-vps.sh

# Скрипт сам поднимает songforge и worker.
# Если версия старая — он убивает процессы и запускает ещё раз.

# Дождаться: === Готово! v2.11.xx ===

curl -s https://sozdaipesnu.ru/api/health


Новый сервер с нуля: FULL-DEPLOY-FROM-SCRATCH.txt
Обычный путь: COMMANDS.txt → update-now.sh
Если GitHub 404: DEPLOY-LOCAL.txt
