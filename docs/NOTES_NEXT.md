# Примечания к следующей правке

> **12.09.2026.** Код **2.11.65**. Нужен деплой.
> Праздники не слать.

---

## Аудит 12 сентября 2026 (не пилить, пока не скажет)

Полный разбор — в чате. Здесь только очередь, без рецепта атаки.

**Деньги / касса**
- Webhook GetPlatinum: проверка подписи слишком широкая (в т.ч. поле из тела как ключ). Закрыть: только API-ключ.
- Две пробные разом с одного аккаунта (гонка счётчика).
- Новый email/MAX = новая проба, потолок 8 с IP в сутки.

**Первая песня (конверсия)**
- Слово «подарок» в брифе парсится как жанр «Рок».
- Ник-модалка без крестика встречает человека из MAX до кнопки «Создать».
- Зависшая генерация не сгорает по таймеру (проба уже списана).
- Если текст не прошёл фильтр — в песню уходит чужой шаблон, не идея человека.

**Доверие / UX**
- На сайте «за минуту», по факту 3–5 мин. FAQ врёт про 30–90 сек и Продамус.
- Кнопки «1 нота» и «5 нот» в модалке ведут на один и тот же пакет.
- Нет тоста при `payment=failed`.
- Cron бэкапа и health-watch на сервере — не подтверждены.

**Не трогать в этом списке:** GetPlatinum clientId, meta TG, календарь, VK-бот.

**Сделано в 2.11.61:** подарок ≠ рок; ник «Позже»; HMAC кассы только API-ключ.

Дальше по аудиту (не в этом релизе): гонка двух проб, зависшая генерация, шаблон текста, кнопки 1/5 нот.

---

## Handoff — 12 сентября 2026 (воронка MAX)

### Сделано
- Путь: обложка → кому → повод → деталь → звук → голос → студия.
- «Начать» / bot_started сбрасывает песню, согласие не трогает.
- «Привет», «сколько стоит», «ок» в бриф не пишем. Голос без кнопки — переспрашиваем.
- Обложка: загрузка файла в MAX, запасной вариант — URL.
- Бриф в студию: кому, повод, деталь, тон, «женский/мужской голос».
- Кнопки + свои слова. Бизнес — боковая дверь.

### После деплоя
- Стереть чат → Начать. Обложка должна быть.
- Пройти путь до студии, глянуть поле идеи.

### Не трогать
- Календарь праздников, VK, Mini Apps, видео.

---

## Handoff — 11 сентября 2026 (MAX С0–С3)

### Сделано (без деплоя)
- Обложка `assets/max-cover.jpg` в первое сообщение (картинка + «Поехали»).
- После согласия сразу «кому», не подарок/бизнес.
- Вторая кнопка — повод: ДР / просто так / скоро праздник.
- Бизнес — боковая дверь, если сам написал «реклама / бренд / джингл».
- «Привет» не сбрасывает шаг.
- В карточке: `occasion_key`, `nudge_step`, `next_nudge_at`.
- Дожим шлёт worker: 2 ч / 24 ч / 4 дня. Тексты разные. Потом молчим.
  Открыл студию — первый пинг пропускаем. Стоп — кнопка на третьем письме.

### Напоминания (решение)
- Слой A эта песня — на проде 2.11.59.
- Слой B готово/не забрал — на проде 2.11.59.
- Слой C праздники — позже; сейчас только храним кому+повод.

---

## Handoff — 11 сентября 2026 (хиты)

### Сделано (без деплоя)
- Убрали статичный HIT_IDEAS (Зиверт / Баста / Кино…).
- Блок «Похожее на хиты» снова берёт топ `/api/explore`.
- «Похожее» кладёт в поле идеи `similar_idea`: идея хита + стиль генерации.
  Нет шаблона «создай в стиле «название»». Нет фамилий звёзд.
- Если промпта нет — запасной текст по жанру.

### Дальше
- Обложка к первому экрану MAX (жест, не логотип) в `screens/`.
- Код бота по MAX-ПРОДАЖНИК.txt одним релизом. 2.11.59 не деплоить отдельно.

---

## Handoff — 10 сентября 2026 (бот MAX)

### Сделано
- Карточка контакта + лог согласий (`messenger_contacts` / `messenger_consents`).
- Бот MAX: ворота «Поехали» → «Кому первую песню?» → настроение → ссылка
  `/api/auth/max?m=...` — на сайте уже вошёл, идея в поле, помощник видит brief.
  Тексты 2.11.56: не визитка студии. Кнопки: Маме / Любимой / Любимому / …
- Webhook: `POST /api/webhooks/max`, подписка при старте, секрет из токена.
- TLS к platform-api2.max.ru: сертификаты НУЦ Минцифры в `backend/certs/`.
- «Стоп» / блок чата — больше не пишем.

### После деплоя (сделано)
- `/api/health` → 2.11.56 и `"max_bot": true`
- Живая проверка текстов — завтра: «Начать» заново

### Завтра (не код, пока не скажет)
- Одна обложка к первому экрану (не видео сразу)
- Скрины — в `screens/`

### Не сделано
- VK-бот тем же мозгом
- Follow-up если затих (1–3 ч / сутки)
- Календарь праздников
- Видео / мини-приложение

---

## Handoff — 4 сентября 2026 (хиты + мессенджеры)

### Хиты (устарело 11.09 — вернули МузПлощадку)
Было: клик «Похожее» подставлял шаблон «в стиле названия»;
потом статичный HIT_IDEAS со звёздами. И то и другое снято.

### Мессенджеры (решение, код не писать)
Первый экран — оферта + соглашение + политика + ПД + рассылка.
Дальше диалог, пока «стоп» / блок. Одна карточка: max/vk/tg/site + brief.
Помощник на сайте читает тот же brief. MAX API: platform-api2.max.ru.
Массовая реклама MAX Bot API — только с договором MAX.
Юр/техника: МЕССЕНДЖЕРЫ-ПЛАН.txt. Маркетинг: МЕССЕНДЖЕРЫ-МАРКЕТИНГ.txt.
Код не пилить. Порядок изучения: живые формулировки → бумага → MAX.

---

## Handoff — 2 сентября 2026 (GetPlatinum checksum v2)

### Что сказали GP
Новая версия API. Checksum больше не в JSON. Считать так:
`X-Checksum = HMAC-SHA256(сырое тело запроса, API-ключ).hex().upper()`

### Сделано в 2.11.43
- Проверяем заголовок `X-Checksum` первым.
- JSON checksum v1 оставлен как запасной.
- IP-fallback пока не убирали (если nginx не прокинет заголовок — ноты всё равно начислятся).

### После деплоя (сделано: прод 2.11.43)
Тестовая оплата 1 ноты. В логах: `signature OK (X-Checksum)`.
Методы `/api/public/v2/pay/...` пока не переключали — только подпись.

---

## Handoff — 2 сентября 2026 (цены)

### Сделано
- Пакеты: 1=149, 3=375, 5=599, 10=899 (те же скидки −16/−20/−40%).
- Источник: `payment_service.py` PACKAGES; сайт, FAQ, соглашение, консультант, гайды.
- Консультант берёт цены из PACKAGES — чтобы не разъехались.
- BETA50 поверх новых цен не рекомендуется (пакет 10 → ~45 ₽/нота).

### После деплоя проверить
- `/api/health` → 2.11.42
- `/api/payment/packages` → 149/375/599/899
- Модалка «Купить ноту», раздел «Пакеты», `/legal/terms`
- Форма GetPlatinum: 149.00 RUB за 1 ноту (не 1.49)

### Не тронуто
- GetPlatinum webhook fallback; clientId; meta TG.

---

## Handoff — 30 июля 2026 (архив)

> Прод **2.11.41**. index.html — реквизиты, русские жанры, cookie-баннер
> (аналитика ещё НЕ подключена); Tailwind — статичная сборка `assets/tailwind.css`.
> После смены CSS-классов в index.html: `npm run build:css`.

---

## Handoff — 30 июля 2026 (конец сессии, сохранись)

### Сделано
- **Футер index.html**: добавлены реквизиты ИП Мошкин М.А. / ИНН / ОГРНИП (были только
  на лендинге, требование юр-гайда EP Legal — п.3 «укажите данные владельца сайта»).
- **Жанры на русском**: `Pop/Rock/Electronic/Lo-Fi` → `Поп/Рок/Электронная/Ло-фай`
  в боковой панели index.html (соответствие 168-ФЗ о русификации + мелкий UX-баг,
  раньше английское слово подмешивалось в русский промпт).
- **Cookie-баннер**: добавлен на index.html и landing/index.html — «Принять все /
  Только необходимые / Отклонить», согласие хранится в localStorage. Аналитика
  (Яндекс.Метрика) **пока не подключена** — баннер подготовлен заранее. При подключении
  любой аналитики её инициализацию нужно обернуть в `if (window.hasAnalyticsConsent())`.
- **Политика конфиденциальности**: раздел 8 (Cookie) переписан под новый баннер
  (было — «продолжая пользоваться сайтом, вы соглашаетесь», это больше не считается
  валидным согласием). HTML-версия перегенерирована: `python scripts/md_legal_to_html.py`.
- **Tailwind CDN → статичная сборка**: убрали `<script src="https://cdn.tailwindcss.com">`
  (варнинг в консоли + лишний JS на клиенте), теперь `<link href="/assets/tailwind.css">`.
  Добавлены `package.json`, `tailwind.config.js` (content: index.html),
  `tailwind.input.css`. Пересборка: `npm run build:css`. `node_modules/` в `.gitignore`.

### Известный риск на будущее
- **GetPlatinum webhook fallback** (см. запись 29.07) — не тронуто, работает как есть.

### Не сделано / на будущее
- Яндекс.Метрика ещё не подключена владельцем (нужно — банер уже готов, ждёт).
- Тот же build-шаг для Tailwind стоит добавить и для `admin.html`, если там тоже
  когда-нибудь понадобится (сейчас admin.html свою вёрстку не через Tailwind CDN тянет).

---

## Handoff — 29 июля 2026 (конец сессии, сохранись)

### Контекст
Повторный аудит безопасности (после предыдущего, где были закрыты 4 блокера:
webhook-подпись, коды логина, гостевой триал, копирайт-права). Все 4 подтверждены
как реально закрытые. Найдено и исправлено ещё три вещи.

### Сделано
- **Rate-limit на `/api/auth/email/verify`** — раньше подбор 6-значного email-кода
  ничем не ограничивался (только запрос кода лимитировался, не проверка).
  Добавлены `RATE_AUTH_VERIFY_EMAIL_LIMIT` (10/15мин) и `RATE_AUTH_VERIFY_IP_LIMIT`
  (20/15мин) в `backend/settings.py` + проверка в `backend/app.py`.
- **XSS в `admin.html`** — функция `esc()` не экранировала кавычки (была через
  `textContent→innerHTML`, из-за чего `data-title="..."` можно было пробить
  названием трека с `"`). Заменена на regex-эскейпинг всех 5 спецсимволов,
  как уже было в публичном `index.html` (`escHtml`).
- **Баг `init_db()`** в `backend/database/db.py` — миграция колонок `users`
  вызывалась до создания самой таблицы `users`, из-за чего инициализация
  падает на полностью чистой БД (новый сервер / восстановление с нуля).
  В проде не проявлялось, т.к. таблица `users` там уже существует.
  Порядок исправлен: сначала `CREATE TABLE users`, потом миграция.
- Все правки задеплоены на VPS через `bash scripts/update-now.sh`, прод
  подтвердил health-check на v2.11.41. Тесты: `74 passed`.

### Известный осознанный риск (не блокер, держать в уме)
- **GetPlatinum webhook fallback** (коммит `v2.11.23`, 15.07.2026): если
  проверка checksum не проходит (официальный алгоритм GP не задокументирован),
  платёж принимается по IP-префиксу (`212.41.13.`) + известному pending order_id.
  Это ослабление контроля, а не забытая дыра — сделано осознанно, т.к. checksum
  часто не сходится на реальных вебхуках. Стоит один раз написать в поддержку
  GetPlatinum за точным алгоритмом подписи, чтобы убрать fallback совсем.

### Не сделано / на будущее
- Код авторизации через `random.randint` вместо `secrets` в `auth_service.py`
  (не критично, но правильнее — `secrets.randbelow`).
- Rate limiter — in-memory, один процесс (осознанно для беты). При переходе
  на несколько PM2-процессов лимиты перестанут работать корректно — понадобится
  Redis-based limiter.

---

## Handoff — 23 июля 2026 (архив)

### Сделано
- Оффер беты: `docs/instrukcii/BETA-OFFER.txt` (BETA50 −50% ×25)
- Соглашение: стоимость ноты от пакета (299 / ~180 за 10), не «только от 299»
- Мессенджеры + TG-форум как Virtuos → план/отложено; прайс 199 не меняли
- Лендинг: review + UTM/meta/favicon/честное демо → `landing/` в GitHub
- Инструкция: `docs/instrukcii/LANDING-HOSTING.txt` (отдельный хостинг РФ)

### Не сделано (владелец)
- Код в GetPlatinum + рассылка тестерам
- Покупка домена/хостинга и заливка ленда
- og:url ленда на свой домен (после появления домена)

### Не пилить
- Meta/OG ради TG; GP clientId; прайс в payment_service без решения

---

(Ниже — архив.)

## Handoff — 22 июля 2026 — архив (TG-карточки, 2.11.41)
