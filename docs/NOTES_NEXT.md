# Примечания к следующей правке

> Зафиксировано: июль 2026. Контекст после деплоя v2.4.1.
> Сессионный статус и тексты для UI: `../SONGFORGE-КОНТЕКСТ.txt` (разделы 12–14).
> **Handoff:** `SONGFORGE-КОНТЕКСТ.txt` §0d–§0f + `ЗАПУСК-ПЛАН.txt` (v2.10.4, план запуска).

---

## Handoff — актуально *(10 июля 2026)*

**Прод:** v2.11.2 → деплой **v2.11.3**. SMTP работает (код в письме).

**Сессия v2.11.3 (замечания владельца 10.07):**
- [x] Темы burgundy + obsidian — ярче rose/copper
- [x] Письмо: ссылка `/?auth=email` → форма ввода кода
- [x] GetPlatinum: понятная ошибка + `GETPLATINUM-ENV.txt`
- [x] Мобильный шрифт чуть крупнее
- [x] Липкий мини-плеер при воспроизведении
- [ ] GetPlatinum ключи в .env на сервере — блокер оплаты
- [ ] UI backlog: галочка, кнопка «Получить код» — § вход D

**Продолжить:** деплой v2.11.3 → `docs/instrukcii/GETPLATINUM-ENV.txt` → тест оплаты.

**Порядок в папке (10.07):** команды — `COMMANDS.txt`; инструкции — `docs/instrukcii/INDEX.txt`.

---

## Почта REG.RU — sm42, noreply *(9 июля 2026, пауза)*

> Полный handoff: `SONGFORGE-КОНТЕКСТ.txt` §0h. Копировать записи: `REG-RU-DNS-ЗАПИСИ.txt`.

**Тариф:** Mail-1, ispmanager, `sm42.hosting.reg.ru`. Сайт на VPS `195.19.20.245` — отдельно.

**Сделано:**
- [x] DNS-домен + почтовый домен `sozdaipesnu.ru` в ispmanager (IP сайта 195.19.20.245)
- [x] DKIM/DMARC — тексты в §0h / `REG-RU-DNS-ЗАПИСИ.txt`
- [x] Ящик `noreply@sozdaipesnu.ru` создан

**Осталось:**
- [x] MX + TXT (SPF, DMARC, DKIM) в DNS
- [x] `SMTP_*` в `.env` → `email_smtp:true` (проверено curl 9.07)
- [ ] **SMTP отправка с VPS падает** — «Получить код» → пауза → ошибка (9.07, владелец)
- [ ] Тест входа: код в письме на личную почту (не dev_code на экране)
- [ ] TG/VK — `telegram_enabled`/`vk_enabled` пока false (`ВХОД-EMAIL-TG-VK.txt`)

---

## SMTP — код не уходит с сервера *(9 июля 2026, баг на проде)*

**Симптом:** `email_smtp:true`, но «Получить код» → 20–45 сек → toast «Не удалось отправить код». API иногда отдаёт HTTP 500.

**Причина:** `SMTP_HOST` в `.env` задан, но connect/login/send с VPS **195.19.20.245** до REG.RU не проходит (неверный host/порт/пароль или блокировка 587).

**Диагностика на сервере:**
```text
cd ~/SongForge && ./venv/bin/python scripts/test_smtp.py
pm2 logs songforge --lines 40 --nostream
```
Инструкция: `SMTP-DIAGNOSTIKA.txt`

**Частый фикс .env:**
- `SMTP_HOST=mail.hosting.reg.ru` вместо `sm42.hosting.reg.ru`
- или порт `465` + `SMTP_USE_TLS=false`
- проверить пароль: вход noreply@ в Roundcube

**Код (v2.11.1):**
- [x] `email_service.py` — ловить timeout/auth, понятные ошибки в лог
- [x] `scripts/test_smtp.py` — перебор host/port
- [x] `index.html` — кнопка «Отправляем...», toast с текстом API
- [ ] Задеплоить на сервер + прогнать test_smtp + поправить .env

**SMTP для .env:** `SMTP_HOST=sm42.hosting.reg.ru`, port 587, user `noreply@sozdaipesnu.ru`, TLS true.

---

## Handoff — архив *(8 июля 2026)*

**Продолжить сессию (старое):** `SONGFORGE-КОНТЕКСТ.txt` §0d, §0g + § «Анализ перед бетой» ниже.

**Запуск (§0f, `ЗАПУСК-ПЛАН.txt`, решение 6.07.2026):**
- **Продамус — отказ.** Целевой провайдер: **GetPlatinum**
- **Бета — с нормальной оплатой** + скидка тестерам (промокод / бета-пакет)
- **Домен+HTTPS — блокер** (заявка в GetPlatinum и бета только на https://домен/)

**Этап 0 (до беты):**
- [ ] Домен → nginx + certbot → `SITE_URL=https://…`
- [ ] URL `/legal/offer`, `/legal/privacy`, `/legal/terms`
- [ ] Интеграция GetPlatinum в `payment_service.py` + webhook с подписью
- [ ] Промокод или бета-пакет со скидкой
- [ ] Тестовая оплата end-to-end

**Этап 1 — бета:** 5–20 тестеров, полный цикл с картой  
**Этап 2 — публичный:** rate limit, бэкап, SMTP, OG → реклама

**Следующие правки кода (приоритет):**
- [ ] GetPlatinum provider (вместо доработки Prodamus)
- [ ] Промокоды / скидка бета
- [ ] `/legal/*` страницы
- [ ] Rate limiting, бэкап — до рекламы

**Уже сделано v2.10.x:** WAL, worker, Redis, S3, атомарные ноты, UI, юрдокументы+оферта.

**v2.10.5 — sunoapi.org (запасной Suno):**
- [x] `SunoApiOrgClient` + `MusicProviderService` (`apipass` | `sunoapi` | `fallback`)
- [x] `music_provider` в `generations` — poll идёт в правильный API
- [x] Webhook-заглушка `/api/webhooks/sunoapi` для `callBackUrl`
- [ ] Пополнить credits на sunoapi.org (ключ проверен, баланс был 0)
- [ ] A/B тест качества ApiPass vs sunoapi на 3–5 песнях

Фазы: `SONGFORGE-КОНТЕКСТ.txt` §0e.

---

## Перед бета-тестом — план *(8 июля 2026, v2.10.6 на sozdaipesnu.ru)*

**Уже на проде:**
- [x] Домен + HTTPS, `SITE_URL=https://sozdaipesnu.ru`
- [x] v2.10.6: цены, GetPlatinum API, `/legal/*`, `fallback_suno`
- [x] Redis, S3, worker, WAL, атомарные ноты

**Блокеры беты (сначала это):**
- [ ] Тестовая оплата end-to-end: кнопка → GetPlatinum → webhook → +ноты
- [x] dev-topup полностью удалён из кода (v2.10.7)
- [x] Деплой v2.11.0 на сервер + SMTP (`email_smtp:true`)
- [ ] Промокод / скидка для тестеров (или фикс. бета-цена в GP)
- [x] Чекбокс согласия при регистрации в `#authModal` (v2.11.1)
- [x] SMTP для email-входа — настроен (REG.RU noreply@)

**Желательно до 5–20 тестеров (надёжность):**
- [ ] Rate limit: auth, create-song, generate-lyrics (slowapi)
- [ ] Лимит 2–3 параллельных генераций на сервере
- [ ] Cron-бэкап `~/SongForge/data/*.db` (ежедневно)
- [ ] Алерты владельцу: ошибки оплаты, webhook, зависшие генерации (Telegram/email — см. § админ-алерты)

**После беты / не блокер первой волны:**
- [x] SMTP + email-вход (v2.11.0) — на проде нужен SMTP в `.env`
- [x] Telegram Login Widget + HMAC verify (v2.11.0) — BotFather `/setdomain`
- [x] VK ID OAuth (v2.11.0) — redirect `https://sozdaipesnu.ru/api/auth/vk/callback`
- [ ] МАКС — позже, отдельная интеграция
- [ ] OG-превью `/t/{id}`, текст в модалке, UI-полировка

---

## Анализ перед бетой — сессия 8 июля 2026 *(зафиксировано, владелец изучает)*

> Полный разбор в чате 8.07.2026. Код v2.10.7 — по запросу в следующей сессии.
> Краткая выжимка также в `SONGFORGE-КОНТЕКСТ.txt` §0g.

### Промокод GetPlatinum

**Сейчас:** промо настроено в ЛК GetPlatinum; в `payment_service.py` → `_init_getplatinum_payment` промокод **не передаётся** (только `amount`, `positions`, `clientParams`, `customParams`).

**Webhook:** начисляет `notes_amount` из `payment_orders` / `PACKAGES` — не по фактической сумме оплаты. Для беты это нормально (скидка на стороне GP, ноты полные).

**Проверка владельца (до приглашения тестеров):**
- [ ] Пакеты → «Перейти к оплате» → на форме GetPlatinum ввести промокод
- [ ] Если скидка применилась и webhook начислил ноты — ок без правок кода
- [ ] Если поля промокода нет / не работает — уточнить API GP или запасной план:
  - [ ] Поле `promoCode` (или аналог) в `init-payment-url` payload
  - [ ] Поле промокода на сайте (Пакеты) → передача в GP
  - [ ] Скрытый бета-пакет в `PACKAGES` (фикс. цена со скидкой)

**Файлы:** `backend/services/payment_service.py`, `index.html` (раздел пакетов)

### Dev-topup — полностью удалён *(v2.10.7, 8 июля 2026)*

**Сделано:**
- [x] Удалены `POST /api/dev/topup`, `DevTopup*`, `DEV_TOPUP_ENABLED`, `dev_tools`
- [x] Убраны 5 кликов по логотипу, `adminAddCredits`, `#adminPlaceholder`
- [x] `deploy-vps.sh` больше не выставляет `DEV_TOPUP_ENABLED=true`

**После деплоя на сервер:**
- [ ] `update-now.sh` → health `"version":"2.10.7"`
- [ ] Опционально: удалить строку `DEV_TOPUP_ENABLED` из `~/SongForge/.env`

**Замена для сбоев:** `/admin` → ± ноты с причиной (audit).

### Админка — выдача нот (±1 и более)

**Уже есть:** `AdminService.adjust_balance()` + `POST /api/admin/users/{id}/balance` + audit `users.balance_adjust`.

**UI:** `admin.html` — кнопка «± ноты» через `prompt()` (можно ввести `1` или `-1`). Support: лимит ±20 за раз.

**v2.10.7 — улучшить UI:**
- [ ] Модалка вместо `prompt`: кнопки **−1 / +1 / +5**, поле «причина»
- [ ] super_admin без лимита ±20 (support — оставить лимит)

**Файлы:** `admin.html`, `backend/services/admin_service.py`

### Объявления / бегущая строка из админки

**Сейчас:** статичные `announce-card` в левом сайдбаре `index.html` (~287–288). Редактирование только через код.

**Нужно (v2.10.7):**
- [ ] Таблица `site_announcements` (text, type, active, starts_at, ends_at)
- [ ] `GET /api/announcements` — публичный список активных
- [ ] `POST/PATCH /api/admin/announcements` — вкладка «Объявления» в `/admin`
- [ ] Фронт: верхняя полоска или бегущая строка под шапкой; опционально замена блока «Афиша»
- [ ] Типы: `info`, `warning`, `maintenance` (техработы)

**Связь:** backlog «Афиша из админки» (§ левый сайдбар).

### Архитектура: одностраничник vs многостраничник

**Факт:** не классический лендинг, а SPA в одном `index.html` — `switchToView()` переключает панели (create, library, explore, profile, payment, legal).

**Отдельно:** `/legal/*`, `/admin`, `/t/{id}`.

**Для беты:** переделка **не блокер**. SEO слабее, но для 5–20 тестеров ок.

**Позже (не срочно):**
- [ ] Hash-роутинг (`/#library`, `/#payment`) — мало работ
- [ ] `track.html` + Open Graph для Telegram/VK (уже в backlog шаринга)
- [ ] Полный фреймворк (Vue/React) — только при росте команды

### Нагрузка сервера — как проверить

**VPS:** ~1 ГБ RAM, диск ~8 ГБ (аудит 03.07.2026).

**Узкие места:** 1 worker uvicorn, блокирующие запросы к ApiPass/GPT, SQLite, нет rate limit, нет лимита параллельных генераций.

**Бета 5–20:** достаточно при условиях ниже.

**Чеклист проверки:**
- [ ] `free -h` — swap не забит постоянно
- [ ] `df -h` — свободно > 1,5 ГБ
- [ ] `pm2 monit` — RAM < 90%, нет частых рестартов
- [ ] 2–3 одновременные генерации — сайт не падает с 500
- [ ] UptimeRobot на `https://sozdaipesnu.ru/api/health` (каждые 5 мин)
- [ ] Rate limit + лимит 2–3 активных генераций (код — backlog)

**Апгрейд VPS:** при OOM, диск < 1 ГБ, регулярные 500 при 3+ генерациях.

### Мониторинг ApiPass и sunoapi

| Источник | Баланс | Ошибки | В коде |
|----------|--------|--------|--------|
| **sunoapi.org** | ЛК + API `GET /generate/credit` | ЛК | `SunoApiOrgClient.get_credits()` — **не в админке** |
| **ApiPass** | Только ЛК apipass.dev | ЛК + `fail_msg` в БД | `createTask` + `recordInfo` only |
| **SongForge /admin** | — | список генераций, `fail_msg` | дашборд: stuck, errors 24h |

**v2.10.7 / желательно до беты:**
- [ ] Виджет «sunoapi credits» на дашборде `/admin` (порог алерта, напр. < 10)
- [ ] Сводка ошибок генерации за 24ч (частично есть в alerts)
- [ ] Опционально: Telegram-алерт владельцу при credits < N или stuck > 0
- [ ] ApiPass: ручная проверка баланса в ЛК; уточнить у поддержки наличие balance API

**Файлы:** `backend/services/sunoapi_org_client.py`, `admin_service.py`, `admin.html`

### План v2.10.7 (следующая сессия — по запросу владельца)

Приоритет после блокеров беты:
1. [x] dev-topup удалён (v2.10.7)
2. [ ] Чекбокс согласия в `#authModal`
3. [ ] Админка: модалка ±1/+5 нот
4. [ ] Объявления из админки (бегущая строка / баннер)
5. [ ] sunoapi credits на дашборде админки
6. [ ] Промокод GP в API — только если тест на форме не прошёл

---

## Студия — кнопки генерации для гостей *(8 июля 2026, v2.10.8)*

**Было:** `createSongBtn`, `generateTextBtn` и др. серые и `disabled` без входа — пользователь не понимал, что делать.

**Сделано (v2.10.8):**
- [x] Кнопки всегда выглядят активными (не `disabled`, без `opacity-50`)
- [x] По клику без входа — модалка `openAuthModal('studio')` с текстом про бесплатную регистрацию и пробную
- [x] Подсказка под кнопкой AI-продюсера для гостя: «Бесплатный вход по email…»
- [x] `generateTextBtn` — более заметный стиль (жёлтая обводка)

**Файл:** `index.html` — `updateStudioGenerateButtons`, `blockSongGenerationIfNeeded`, `#authModalTitle` / `#authModalSubtitle`

---

## Маркетинг — тексты входа для гостя *(9 июля 2026, запрос владельца)*

**Замечание:** при первом клике без входа («Создать песню», «Сгенерировать текст») открывается модалка входа. Текущие формулировки **не цепляют**, звучат **в лоб** и как ограничение, а не как приглашение. То же в **Профиле → Гость** и в подсказках под кнопками студии.

**На проде (v2.10.8) ещё может быть старый вариант:**
- заголовок: «Создайте бесплатный аккаунт»
- подзаголовок: «Чтобы сгенерировать песню, войдите по email… На аккаунт даётся 1 пробная: 2 варианта по 30 секунд»

**В коде main (v2.11.0) уже другой черновик, но проблема та же** — везде «пробная / 30 сек / нельзя без входа» вместо выгоды и эмоции.

### Где править (все точки контакта гостя)

| Место | Сейчас (смысл) | Файл |
|-------|----------------|------|
| Модалка студии | `authModalStudioCopy` — «Твоя песня — в один клик» / «Осталось войти…» | `index.html` — `openAuthModal('studio')` |
| Модалка по умолчанию | «Вход в аккаунт» / «превью, история, фонотека…» | `authModalDefaultCopy` |
| Профиль → Гость | `#guestLimitText` — «1 пробная… 2 превью… без входа нельзя» | `index.html` ~488 |
| Под кнопкой AI-продюсера | `#producerCreateHint` (гость) — «Бесплатный вход… 1 пробная… 30 сек» | `updateStudioTrialHints()` |
| Шаг 3 / расширенный | `#step3Hint` (гость) — «откроется быстрый вход…» | `updateStudioTrialHints()` |
| Афиша слева | `.announce-card` — «1 пробная… 2 варианта по 30 сек» | `index.html` ~290 |
| Пакеты (гость) | «1 пробная генерация на аккаунт • 1 нота = …» | `index.html` ~520 |
| FAQ / консультант | «1 пробная… 30 сек» в ответах | `index.html` FAQ, `consultant.py` |

**Принцип (согласовать перед вёрсткой):**
- Сначала **выгода и эмоция** («услышь свою песню», «идея → трек за минуты»), потом условия — **мелко или вторым абзацем**
- «30 секунд» и «1 пробная» — **не в заголовке модалки**; лучше: «попробуй бесплатно» / «первая песня — в подарок»
- Избегать отрицаний в лоб: «без входа нельзя», «даётся 1 пробная»
- Один тон на всех экранах: модалка, профиль, подсказки под кнопкой
- Честность сохранить (юридически и по продукту) — детали в FAQ / мелкий текст под кнопкой входа

### Черновики направления (не финал — выбрать с владельцем)

**Модалка при клике «Создать песню» / «Сгенерировать текст»:**

| Вариант | Заголовок | Подзаголовок |
|---------|-----------|--------------|
| A — эмоция | «Твоя песня уже ждёт» | «Войди за 10 секунд — и услышишь, как звучит твоя идея. Первая генерация бесплатно.» |
| B — любопытство **(выбрано 9.07)** | «Сейчас начнётся магия» | «Остался один шаг: быстрый вход через Telegram, VK или email. Подарок новичкам — первая демо-версия твоей идеи.» |
| C — соцдоказательство | «Тысячи уже создали свою» | «Присоединяйся — вход бесплатный, карта не нужна. Сразу после входа запустим твою первую песню.» |

**Профиль → Гость (`#guestLimitText`):**
- «Войди — и твоя первая песня уже в студии. Telegram, VK или email, без пароля.»
- Мелко под кнопкой: «Новичкам — одна бесплатная генерация с двумя вариантами на выбор.»

**Под кнопкой AI-продюсера (гость):**
- «Нажми «Создать песню» — откроется быстрый вход. Идея → трек за несколько минут.»

**Микро-фишки (опционально, v2.12+):**
- [ ] В модалке: 3 иконки «Telegram / VK / Email — 10 сек»
- [ ] Строка социального доказательства: «Уже N песен создано сегодня» (из админ-дашборда или статика на бету)
- [ ] Ротация подзаголовков (2–3 варианта, как `studioTipsPool`)
- [ ] После входа — короткий toast «Добро пожаловать! Запускаем твою первую песню…» вместо сухого «Вход выполнен»

**Задачи:**
- [x] Согласовать финальные тексты — **вариант B** (9.07.2026)
- [x] Внедрить в `authModalStudioCopy`, `#guestLimitText`, `updateStudioTrialHints()`, `.announce-card`
- [ ] Пройтись FAQ + `consultant.py` — единый тон (мелко, не блокер)
- [ ] После деплоя — убедиться, что старый текст «Создайте бесплатный аккаунт» нигде не остался на проде

**Файлы:** `index.html`, `backend/services/consultant.py`

---

## Помощник — «Привет» на каждый вопрос *(9 июля 2026, запрос владельца)*

**Симптом:** чат «Спросить помощника» на каждый новый вопрос начинает ответ с «Здравствуйте» / «Привет», как будто диалог только что открыли — даже после 3–5 сообщений подряд. Выглядит неестественно и раздражает.

**Причина в коде:**

1. **Фронт** (`index.html` → `sendChat`): на API уходит только **текущее** сообщение + опционально `context` (идея из студии). История в `#chatMessages` **не передаётся**.
2. **API** (`POST /api/consultant/chat`): `ConsultantRequest` — поля `message`, `context`; истории нет.
3. **Бэкенд** (`consultant.py` → `reply`): один вызов `YandexClient.complete(system, user_text)` — каждый раз «чистый» диалог из 2 сообщений.
4. **Yandex** (`yandex_client.py`): `messages: [system, user]` — без цепочки `assistant`/`user`.
5. В `SYSTEM` нет правила «не здороваться повторно»; модель по умолчанию вежливо приветствует.

Статичное приветствие в виджете (`#chatMessages` первая плашка) — ок; проблема в **каждом ответе GPT**.

### Цель правки

Помощник ведёт себя как живой собеседник: приветствие **один раз** при открытии чата; дальше — сразу по делу, без повторных «Здравствуйте», с учётом предыдущих реплик.

### План (v2.11.1 или по запросу)

**A. История диалога (обязательно)**

- [ ] Фронт: массив `chatHistory` (`{role:'user'|'assistant', text}`), пополнять в `sendChat` / `appendMsg`
- [ ] Отправлять последние **6–10 реплик** (3–5 пар) в теле запроса — лимит по токенам
- [ ] Опционально: `sessionStorage` — не терять контекст при перезагрузке страницы в той же вкладке
- [ ] Кнопка «Начать заново» в шапке чата — сброс истории + одна приветственная плашка

**B. API и Yandex**

- [ ] `ConsultantRequest.history: list[ChatTurn]` (`role`, `text`)
- [ ] `YandexClient.complete_chat(messages)` или расширить `complete` — цепочка `system` + N×(`user`|`assistant`)
- [ ] `consultant.reply(message, context, history)` — собирает messages для Yandex

**C. Скрипт общения (system prompt + постобработка)**

Дополнить `ConsultantService.SYSTEM` (черновик правил):

```
Тон: дружелюбный эксперт студии, на «ты», без канцелярита.
• Первое сообщение пользователя в сессии — можно коротко поприветствовать (1 фраза).
• Все следующие ответы — БЕЗ «привет», «здравствуйте», «рад помочь» в начале; сразу ответ по сути.
• Не пересказывай вопрос целиком; не повторяй одно и то же из FAQ, если уже отвечал в этом диалоге.
• Ответ 2–4 предложения; списки — только если реально нужны.
• Если пользователь уточняет — опирайся на предыдущие реплики («как ты писал…»).
• Эмодзи — максимум 1 на ответ, не в каждом.
```

- [ ] Постобработка `strip_redundant_greeting(reply, is_follow_up)` — срезать ведущие «Привет!» / «Здравствуйте!» если `len(history) > 0` (страховка, если модель всё равно поздоровалась)

**D. Контекст сессии (желательно)**

- [ ] В `context` передавать: `logged_in`, `balance`, `trial_left`, `studio_mode` — чтобы не путать гостя и пользователя с нотами
- [ ] Позже: грузить факты из `docs/public/USER_GUIDE.md` (см. § документация)

**E. Тесты**

- [ ] `test_consultant_history`: второй запрос с history → ответ не начинается с приветствия (эвристика или mock)
- [ ] Smoke: `ConsultantRequest` с пустой/непустой history

**Файлы:** `index.html` (`sendChat`, `#chatWidget`), `backend/models.py`, `backend/services/consultant.py`, `backend/services/yandex_client.py`, `backend/app.py`, `tests/test_smoke.py`

**Не блокер беты** — UX-полировка; можно после чекбокса auth и оплаты.

---

## Вход — чекбокс согласия *(9 июля 2026, замечание владельца — приоритет перед бетой)*

> Обновлено по обратной связи: галочка «не там»; при регистрации её нет.

### Проблема (как видит пользователь)

| Место | Сейчас | Вердикт владельца |
|-------|--------|-------------------|
| **Политика конфиденциальности** (`#section-privacy`, низ страницы) | Блок «Согласие на обработку ПДн» + `#consentCheckbox` | **Ерунда** — непонятно, зачем галочка в конце текста политики; пользователь туда не заходит ради регистрации |
| **Пользовательское соглашение** (`#section-agreement`) | Галочки нет | **Ок** — не нужна |
| **Публичная оферта** (`#section-offer`) | Галочки нет | **Ок** — акцепт оферты при оплате, не при регистрации |
| **Регистрация / вход** (`#authModal`) | Галочки **нет** | **Нужна здесь** — блокер беты |

**В коде:** `#consentCheckbox` нигде в JS не используется — ни на что не влияет, только путает.

### Цель правки (v2.11.1 или по запросу)

**A. Убрать ложную галочку из политики**
- [x] Удалён дубль политики из index.html (v2.11.1 — только /legal/*)
- [ ] Оставить только текст политики; при желании — статичный абзац про согласие **без** checkbox
- [ ] Не дублировать галочку в соглашении и оферте

**B. Добавить галочку в `#authModal` (перед «Получить код» / TG / VK)**

Черновик текста (согласовать при вёрстке):

> Я принимаю [Пользовательское соглашение](/legal/terms) и [Политику конфиденциальности](/legal/privacy) и даю согласие на обработку персональных данных.

- [x] Новый `#authConsentCheckbox` в модалке (v2.11.1)
- [x] Ссылки `/legal/terms`, `/legal/privacy` — `target="_blank"`
- [x] Оферта не в тексте регистрации
- [x] Кнопки входа disabled без галочки
- [x] `openAuthModal()` сбрасывает галочку
- [ ] Опционально: `terms_accepted_at` в БД при первом входе

**C. Проверка**
- [ ] Гость: без галочки нельзя запросить код
- [ ] В политике конфиденциальности интерактивной галочки больше нет

**Файлы:** `index.html` — `#authModal`, `#section-privacy`, `openAuthModal`, `authEmailSubmitBtn`, `authVkBtn`, Telegram widget callback

**Связь с юрблоком:** галочку в `#section-privacy` убрать; ссылки в `#authModal` — на `/legal/terms` и `/legal/privacy` (уже есть отдельные URL).

### D. Замечания владельца после v2.11.2 *(9 июля 2026)*

**Согласие на ПДн → та же ссылка, что и «Политика» (`/legal/privacy`)**

- **Юридически:** не ошибка. Согласие на обработку ПДн по 152-ФЗ обычно даётся **в связке с политикой** — там же цели, состав данных, права субъекта. Отдельный URL на согласие не обязателен.
- **UX:** две ссылки на один документ — избыточно; пользователь справедливо путается.
- **Варианты на следующую правку (согласовать текст):**
  - [ ] **A (рекомендуется):** одна ссылка на политику, без второй: «… принимаю [Соглашение] и [Политику конфиденциальности], включая согласие на обработку персональных данных» — без третьей ссылки
  - [ ] **B:** якорь в политике `#consent` (абзац про согласие в `privacy.html` + `id` в `legal_pages.py`) — вторая ссылка ведёт на `/legal/privacy#consent`
  - [ ] **C:** отдельная страница `/legal/consent` — только если юрист попросит отдельный документ

**Кнопка «Получить код» при первом входе**

- Схема **email + одноразовый код** (passwordless) — нормальная практика (Telegram-боты, банки, Notion и т.п.). Регистрация и повторный вход — **один сценарий**: ввёл email → код → аккаунт создан или восстановлен.
- Замечание владельца: для новичка «Получить код» звучит непонятно («зачем код, я же регистрируюсь?»).
- **Варианты копирайта (v2.11.3+, только UI):**
  - [ ] Шаг 1: кнопка **«Продолжить»** или **«Отправить код на почту»** + мелкая подпись под полем: «Код придёт на email — это и вход, и регистрация, пароль не нужен»
  - [ ] Шаг 2 (после запроса): кнопка **«Войти»** (уже есть в JS)
  - [ ] Placeholder email: `ваш@email.ru` вместо `your@email.com`
- **Тишина после клика:** да, сейчас SMTP с VPS заблокирован (тикет REG.RU) — долгое ожидание и ошибка; после разблока — письмо на почту.

**Файлы:** `index.html` — `#authConsentCheckbox`, `#authEmailSubmitBtn`, `#authModalSubtitle`, `openAuthModal`

---

## Юрдокументы — дубль в index.html vs /legal/* *(9 июля 2026, вопрос владельца)*

**Уже есть отдельно (не трогать):**
- `GET /legal/terms`, `/legal/privacy`, `/legal/offer` — `backend/legal_pages.py`
- Тексты из `.md` → `scripts/legal_html/*.html` (GetPlatinum, внешние ссылки)

**Дубль:** тот же текст **вшит** в `index.html` → `#legalPanel` (~600+ строк) + `#consentCheckbox` внизу политики.

**Можно вынести сейчас** (без полного редизайна SPA) — вариант **A**, одна сессия:
- [x] Удалены тяжёлые секции из index.html (v2.11.1)
- [x] Футер и `#authModal` → `/legal/*`
- [x] Раздел «Инфо»: `#section-docs` + FAQ + поддержка

**Позже (полная переделка):** hash-роутинг, fetch фрагментов, отдельный шаблон — не блокер беты.

**Рекомендация:** вариант A сейчас вместе с галочкой в `#authModal` — меньше путаницы, один источник правды (`scripts/legal_html`).

---

## Админ — алерты об ошибках *(8 июля 2026, идея владельца, backlog)*

**Сейчас:** `/admin` — дашборд, генерации, пользователи, витрина, `admin_audit_log` (действия админов). **Автоуведомлений** о сбоях на email/Telegram владельцу **нет**.

**Желательно до/во время беты:**
- [ ] Webhook GetPlatinum failed / ignored — лог + уведомление (Telegram-бот владельца или `ADMIN_ALERT_EMAIL`)
- [ ] Генерация `failed` после списания ноты — сводка в админке + опционально TG
- [ ] Зависшие `generating` > N минут — счётчик на дашборде (частично есть) + алерт
- [ ] `pm2` restart / health down — внешний монитор (UptimeRobot на `/api/health`)

**Не срочно:** полноценный Sentry, отдельная вкладка «Ошибки» с stack trace.

---

## Сайдбар и МузПлощадка — иконки, бейджи чарта, футер *(июль 2026, v2.10.3)*

**Замечание владельца:**
- У заголовков **Топ чарта**, **Идея дня**, **Жанры**, **Похожее на хиты** — «крестики» вместо иконок
- Бейджи **1–3** на обложках треков: чуть крупнее, в левый верхний угол картинки (не перекрывать названия)
- Внизу страницы убрать **ИП Мошкин М.А.** — реквизиты остаются в Соглашении / Конфиденциальности

**Причина крестиков:** `fa-regular` для глифов, которых нет в free regular (crown, dice-d6, chart-simple, fire) → Font Awesome рисует пустой квадрат/крестик.

**Сделано в v2.10.3 (`index.html`):**
- [x] Топ чарта → `fa-solid fa-trophy`
- [x] Идея дня → `fa-solid fa-lightbulb` + кнопка `fa-solid fa-shuffle`
- [x] Жанры → `fa-solid fa-compact-disc`
- [x] Похожее на хиты → `fa-solid fa-fire`
- [x] `.explore-rank-badge` — 34×34px, `top/left: 4px`, `z-index: 3`
- [x] `.sidebar-rank-cover` — `position: absolute`, 28×28px, угол обложки
- [x] Футер: `© 2026 СоздайСвоюПесню` без ИП в строке ссылок

**Правило на будущее:** в сайдбаре и шапке — только `fa-solid` / проверенные `fa-regular` (heart, user, comment, …).

---

## Студия — убрать «2 варианта по 30 сек» под кнопкой создания *(июль 2026, v2.10.2 — сделано)*

**Симптом:** под кнопкой «Создать песню» в студии всегда видно примечание про **2 варианта по 30 секунд / бесплатное превью**. Пользователь считает это лишним: 30 сек — **только первая пробная**; дальше песня создаётся **только при наличии и трате 1 ноты**, и это **полные** треки (2 варианта в фонотеку).

**Модель (актуально):**
- 1 пробная после входа → 2 превью по 30 сек
- Дальше: 1 нота списывается **при создании** → 2 **полных** варианта в фонотеку

**Что убрать / не показывать постоянно на главном экране студии:**

| Где (`index.html`) | Сейчас | Действие |
|--------------------|--------|----------|
| `#producerModeContent` под `#createSongBtn` (~355) | `⚡ Бесплатное превью • 2 варианта по 30 сек • полные версии — 1 нота` | **Убрать** или показывать только если `guestRemaining > 0` |
| `#step3Block` бейдж (~422) | `Бесплатное превью` | **Убрать** после пробной; при нотах — без бейджа или «1 нота» |
| `#step3Block` абзац (~424) | `2 варианта по 30 секунд бесплатно… 1 нота при скачивании` | **Исправить:** не «при скачивании», а при создании; убрать 30 сек если пробная использована |
| `#step3Block` подпись (~428) | `Два варианта по 30 сек — выбери лучший` | **Убрать** или заменить на контекстную (пробная vs нота) |

**Желаемое поведение UI:**
- [x] Пробная доступна (`guestRemaining > 0`): коротко «1 пробная: 2 превью по 30 сек» — **только тогда** *(v2.10.2)*
- [x] Пробная использована, есть ноты: без упоминания 30 сек; кнопка как сейчас (списание при создании) — **без лишнего текста внизу** или одна строка «1 нота = 2 полных варианта» *(v2.10.2)*
- [x] 0 нот: кнопки серые (уже есть), без текста про 30 сек *(v2.10.2)*

**Не трогать в этой правке (другой контекст):**
- Модалка результата `updateResultModalCopy()` — там превью/полные по факту генерации
- История «Превью 30 сек • не куплено» — для старых непокупленных
- Юридические блоки / FAQ

**Файл:** `index.html` — `#producerModeContent`, `#step3Block`; опционально JS `updateStudioGenerateButtons` / `updateProfileAuthUI` для динамической подписи.

---

## Экран генерации — прогресс и таймер *(июль 2026, замечание владельца)*

**Симптом:** при долгой генерации (400+ сек) полоска **застывает на ~88%** — выглядит как «почти готово», хотя Suno ещё работает.

**Важно (решение владельца):**
- **Не** писать в UI названия сторонних сервисов (ApiPass и т.п.)
- Подпись о времени оставить: **«Обычно примерно 3–5 минут»** (как сейчас в `genMsgs`)

**Причина в коде:** проценты **не от Suno** — искусственный таймер (`showGenerating`: cap 88% в фазе `polling`, `setGeneratingStatus` до 92). Таймер «Прошло N сек» честнее полоски.

**На следующую правку — продумать синхронизацию:**
- [x] Вариант A: убрать `%`, оставить только таймер + фразы из `genMsgs` *(v2.10.2)*
- [x] Вариант B: полоска «пульс» без процентов (indeterminate), 100% только в `hideGenerating({success:true})` *(v2.10.2)*
- [x] Вариант C: привязать подпись к фазе (`preparing` / `polling`), без залипания на 88% *(v2.10.2)*
- [x] Не показывать «Почти готово» / высокий % пока API не вернул success *(v2.10.2)*

**Файлы:** `index.html` — `showGenerating`, `setGeneratingStatus`, `runMusicGeneration`, `finalizeMusicPoll`

---

## Аномалия: MP3 7:59, тишина / писк после песни *(июль 2026, первый раз)*

**Симптом:** генерация 400+ сек; оба варианта в UI **7:59**; реальная песня ~4 мин, дальше **тишина** (A) или **писк** (B).

**Вероятно:** артефакт **генератора** (метаданные `duration` завышены, в файл дописан хвост до лимита ~8 мин). Не баг SongForge UI напрямую.

**Что проверить при повторе:**
- [ ] Сырой URL с CDN / S3 — длительность в плеере вне сайта
- [ ] `duration_a/b` в БД `generations` vs фактическое аудио
- [ ] Лог worker: не портит ли S3-зеркалирование файл
- [ ] Записать `task_id`, `production_id`, промпт — для тикета посреднику генерации

**Возможные меры (backlog):**
- [ ] Отображать длительность из метаданных плеера после `loadedmetadata`, не только с API
- [ ] Опционально: обрезка тишины на бэкенде (ffmpeg) — тяжело, не срочно
- [ ] В админке: кнопка «скачать сырой MP3» для разбора

---

## Модалка результата — UI после генерации *(июль 2026)*

### 1. Бегунок / статус при скролле

**Симптом:** при прокрутке модалки `#resultModal` сбоку мешает отображение статуса (бегунок скролла / элемент справа).

**Кандидаты в коде:**
- `#modalCreditsBalance` — баланс нот справа в шапке модалки
- `variant-badge` «Выбран» (`position:absolute` на карточке)
- `#variantATimer` / `#variantBTimer` — таймер воспроизведения

**Задача:**
- [ ] Убрать или упростить мешающий элемент без поломки модалки
- [ ] Проверить `max-h-[92vh] overflow-y-auto` на внутреннем контейнере — возможно вынести шапку из скролла (фикс. заголовок, скролл только карточек)

**Файл:** `index.html` — `#resultModal`, CSS `.variant-card`, `.variant-badge`

### 2. «Посмотреть текст» — не тот текст

**Симптом:** в расширенном режиме пользователь **редактировал** текст в `#lyricsEditor`, в Suno ушла **правка**, песня с правильным текстом — но «Посмотреть текст» показывает **старый** текст ЯндексGPT.

**Причина:** `showResultModal` → `lastResult.lyrics = currentGeneratedLyrics`; при старте музыки в `createSongExpert` в API уходит `ed.value`, но **`currentGeneratedLyrics` не обновляется**.

**Исправление:**
- [ ] Перед `music/start` / `showResultModal`: `currentGeneratedLyrics =` текст из редактора (или из `currentProduction.lyrics` с сервера)
- [ ] `modalShowLyricsBtn` → показывать `lastResult.lyrics` после синхронизации
- [ ] Для AI-продюсера: lyrics из ответа `create-song` (уже в `data.lyrics`)

**Файлы:** `index.html` — `createSongExpert`, `showResultModal`, `modalShowLyricsBtn`

### 3. Кнопка «В фонотеке» → на самом деле скачивание

**Симптом:** после покупки / prepaid кнопка `#variantADownload` / `#variantBDownload` показывает **«В фонотеке»**, по клику — **`downloadTrack`** (скачивание MP3), не переход в фонотеку.

**Сейчас:** `updateVariantDownloadButtons()` — при `purchased`: `innerHTML='… В фонотеке'`, `onclick=downloadTrack`.

**Исправление:**
- [ ] Текст кнопки: **«Скачать»** (или «Скачать MP3») — для prepaid/purchased
- [ ] Переход в фонотеку — только отдельная кнопка `#modalOpenLibraryBtn` («Открыть фонотеку»)
- [ ] Иконка: `fa-download`, не `fa-check` + «В фонотеке»

**Файл:** `index.html` — `updateVariantDownloadButtons`, при необходимости `purchaseVariantToLibrary`

---

## Модалка входа — текст под заголовком *(июль 2026)*

**Было:** «После входа — **неограниченные** превью, история генераций и баланс на сервере.»

**Проблема:** слово «неограниченные» вводит в заблуждение (пробная генерация одна, превью 30 сек).

**Сделано (v2.5.6+):** «После входа — превью генераций, история и баланс нот на сервере.»

- [x] `#authModal` в `index.html` (~стр. 959)

---

## Студия — ложный экран генерации при 0 нот *(июль 2026)*

**Симптом:** при 0 нот API отвечает 403 → редирект на оплату, но экран «Создаём твой лучший трек» остаётся с бесконечным таймером (генерация в ApiPass не стартовала).

**Причина:** `showGenerating()` вызывался до запроса; при 403 `handleGuestLimitError()` бросал исключение **до** `hideGenerating()`.

**Сделано (v2.5.8+):**
- [x] При 0 нот (и без пробной) — **генерация не стартует**: ни запрос, ни экран ожидания (`blockSongGenerationIfNeeded` + блок в `showGenerating`)
- [x] Кнопки «Создать песню» неактивны, пока нельзя генерировать (`updateStudioGenerateButtons`)
- [x] При 403 с сервера — сначала `hideGenerating()`, потом оплата
- [x] `resetStaleGeneratingUI` — только страховка для старых сессий / если UI уже успел «зависнуть»
- [x] Исключение: **1 пробная генерация** на аккаунт разрешена при 0 нот (по продукту)

**Файл:** `index.html` — `runMusicGeneration`, `createSongExpert`, `switchToView`

---

## Студия — генерация текста без входа / без нот *(июль 2026, замечание пользователя)*

**Симптом:** кнопки «Создать песню» (`#createSongBtn`, `#createDemoBtn`) заблокированы при отсутствии входа или нот (v2.5.9), но **«Сгенерировать текст песни»** (`#generateTextBtn`) и **«Ещё раз»** (`#regenLyricsBtn`) в Расширенном режиме по-прежнему активны и вызывают ЯндексGPT бесплатно.

**Кто может сейчас:**
- Гость без входа — музыку создать нельзя, **текст — можно**
- Пользователь с 0 нот (пробная использована) — музыку нельзя, **текст — можно**

**Почему:** `updateStudioGenerateButtons()` и `blockSongGenerationIfNeeded()` затрагивают только `createSongBtn` / `createDemoBtn`. Обработчики `#generateTextBtn` и `#regenLyricsBtn` не вызывают `canStartSongGeneration()`.

**Бэкенд без проверки:** `POST /api/produce`, `POST /api/generate-lyrics` — только `guest_id`, без сессии и баланса.

**Сделано (v2.9.31+):**
- [x] Та же логика, что у музыки: `canStartSongGeneration()` → `'auth'` / `'payment'` / `'ok'`
- [x] В `updateStudioGenerateButtons()` добавить `#generateTextBtn`, `#regenLyricsBtn`; при блокировке — серые кнопки + tooltip
- [x] В начале клика `#generateTextBtn` и `#regenLyricsBtn` — `blockSongGenerationIfNeeded()`
- [x] На бэкенде: `/api/produce` и `/api/generate-lyrics` с `_assert_can_generate` (401/403)
- [x] Убран клиентский fallback с фейковым текстом при ошибке API
- [x] Исключение как у музыки: **1 пробная генерация** на аккаунт при 0 нот — текст тоже разрешён

**Файлы:** `index.html` — `generateTextBtn`, `regenLyricsBtn`, `updateStudioGenerateButtons`; `backend/app.py` — `produce_song`, `generate_lyrics_endpoint`

---

## Фонотека — пагинация по 9 треков *(июль 2026)*

**Задача:** не бесконечная прокрутка при большом числе песен — страницы по 9 карточек, сетка 3×3.

**Сделано (v2.5.7):**
- [x] `#libraryTracksList` — `grid-cols-3`, максимум 9 карточек на экране
- [x] `#libraryPagination` — «Предыдущие» / «Следующие», подпись «1–9 из 27»
- [x] Пагинация скрыта, если треков ≤ 9
- [ ] То же для вкладки «История» — по запросу, если понадобится

**Файл:** `index.html` — `renderLibrary`, `LIBRARY_PAGE_SIZE = 9`

---

## Навигация — шапка и разделы

### Переименование «Библиотека» → «Фонотека»

В верхней шапке (Студия, ~~Библиотека~~, Исследовать, Профиль, Пакеты) пункт **«Библиотека»** → **«Фонотека»**.

- [x] Шапка (`header`): ссылка «Библиотека» → «Фонотека» *(июль 2026)*
- [x] Нижняя мобильная навигация (`bottom-nav`) — уже «Фонотека»
- [ ] Заголовки внутри `#libraryPanel` — уже «Моя фонотека» в табе; унифицировать весь UI
- [ ] Пройтись по текстам: модалки, FAQ, юридические блоки — где речь о *личных* купленных треках, писать «фонотека»; где общая лента — отдельно (см. «Исследовать»)

**Файл:** `index.html` — шапка ~стр. 125, `switchToView('library')` (id панели можно оставить `libraryPanel` для совместимости кода).

### Раздел «Исследовать» — план

Сейчас `#explorePanel` — заглушка с демо-карточками (`exploreEx`, `renderExplore`).

**Идея (согласовано):** здесь будет **публичная лента** — опубликованные треки пользователей с рейтингами. Название «Исследовать» пока оставляем; переименуем позже, когда определимся (варианты: «Сообщество», «Чарт», «Лента»). Часть соцсети, референс «Моя песня».

- [ ] Переосмыслить название в шапке (пока «Исследовать»; варианты позже: «Сообщество», «Чарт», «Лента»)
- [ ] Лента публичных треков: обложка, название, автор, рейтинг, прослушивание
- [ ] Связь с блоком «Топ сообщества» в левой панели студии
- [ ] Кнопка «Опубликовать» из личной фонотеки (после покупки трека)
- [ ] Backend: `public_tracks`, рейтинги, комментарии — см. раздел «Социальная сеть»

**Пока не менять** логику `explore` в коде — только зафиксировать план; переименование «Библиотека» → «Фонотека» — ближайшая правка UI.

---

## Личный кабинет — настройки профиля

В разделе «Профиль» нужен блок **Настройки**:

- [x] **Имя пользователя** — отображаемое имя (редактируемое, отдельно от email) *(v2.2.0)*
- [x] **Аватар** — загрузка и смена фото профиля *(v2.2.0)*
- [x] Сохранение настроек в БД (таблица `users`: `display_name`, `avatar_url`) *(v2.2.0)*
- [x] Превью аватара в шапке сайта и в профиле *(v2.2.0)*
- [x] Валидация: размер файла, форматы (jpg, png, webp) *(v2.2.0)*

**Текущее состояние (v2.2.0):** блок «Настройки» в профиле, `PATCH /api/profile`, `POST /api/profile/avatar`.

### Плеер — обложка при воспроизведении *(v2.2.1)*

- [x] Правый плеер: `#playerCover` подставляется из трека (фонотека, история)
- [x] `GET /api/history/{id}/preview` — поле `image_url`
- [x] История: `image_url_b` для варианта B

---

## Социальная сеть — план развития

Цель: превратить SongForge в платформу с элементами соцсети, **по аналогии с сервисом «Моя песня»**.

### Публикация треков

- [ ] Пользователь может **выложить** свою песню в общую ленту / каталог
- [ ] Публичная страница трека: обложка, название, автор, прослушивание
- [ ] Настройка приватности: только я / публично

### Рейтинги и взаимодействие

- [ ] **Рейтинг** треков (лайки, звёзды или голосование)
- [ ] **Комментарии** к трекам
- [ ] Топ / чарт популярных песен сообщества (заготовка уже есть в UI — «Топ сообщества»)

### Профиль в соцсети

- [ ] Публичный профиль автора: имя, аватар, список выложенных песен
- [ ] Подписки / подписчики (опционально, позже)

### Технические ориентиры (на будущее)

- Новые сущности: `public_tracks`, `ratings`, `comments`
- Модерация контента и жалобы
- Связь с существующей библиотекой: трек из «Моя фонотека» → «Опубликовать»

---

## Экран генерации — факты и прогресс

Блок «Создаём твой лучший трек…» + интересные факты внизу (`#funFact`, `#generatingMsg`).

### Проблема 3: «Проверка 1, Проверка 2…» (v2.4.1, замечено 02.07.2026)

**Сейчас:** при polling Suno подзаголовок `#generatingSubMsg` показывает `Проверка N` (номер попытки API).

**Где:** `index.html` — `runMusicGeneration` и `createSongExpert`, callback `pollMusicStatus`:
`setGeneratingStatus(hint, 'Проверка '+n, pct)`

**Нужно (следующая правка, v2.4.2 или v2.5):**
- [x] Убрать технический счётчик из UI *(v2.4.2)*
- [ ] Подзаголовок: ротация `genMsgs[][1]`, фиксированная фраза («Обычно 1–3 минуты»), пусто, или анимация точек
- [ ] Заголовок `#generatingMsg` — оставить `progress_hint` / «Создаём твой лучший трек…»
- [ ] `#funFact` и `pulse-logo` — оставить

### Проблема 4: тексты режимов студии — слишком много, не для клиента *(02.07 + июль 2026)*

**Было:** пользователи не понимали разницу AI-продюсер vs Расширенный.

**Сделано (v2.9.x):** длинные честные тексты в карточках + accordion «Как получить лучший результат».

**Новое замечание владельца (июль 2026):** на первом экране **слишком много текста** — ломаем пользователя до старта. Нужно **кратко, чётко, визуально проще**, клиентоориентированно.

**Сделано (v2.9.31, §13b):**
- [x] `#modeProducerCard` / `#modeExpertCard` — заголовок + 1 строка + бейджи
- [x] `#ideaInputHint` — одна короткая строка, разная по режиму
- [x] `#studioQualityTips` — убран с главного экрана
- [x] `#expertModeContent` — вступление в 1 строку
- [x] Placeholder `#ideaInput` — разный для producer vs expert
- [x] **Расширенный:** `IDEA_PLACEHOLDER_EXPERT`, `IDEA_HINT_EXPERT` — короче, без BPM/synth-pop
- [x] **Расширенный:** `#customDescriptionInput` + пример — по-русски
- [x] **Расширенный:** `#artistRef` placeholder — «В духе Зиверт или Басты…»
- [x] `studioTipsPool`, `funFacts` — вычищен EN-жаргон
- [x] Честность и клиентский тон сохранены

**ТЗ и черновик направления:** `SONGFORGE-КОНТЕКСТ.txt` §13b

**Файлы:** `index.html` — `IDEA_*`, `#customDescriptionInput`, `#artistRef`, `studioTipsPool`

---

## Hero — «мгновение» → «мгновенье» *(июль 2026, запрос владельца)*

**Задача (следующая редакция):** главный заголовок студии — **«Твоя песня за мгновенье»** (мягкий знак **ь**, не «мгновение»).

**Аудит по репозиторию (5 июля 2026):**

| Где | Сейчас | Действие |
|-----|--------|----------|
| `index.html` — `#mainHero` `h1` (~стр. 315) | `ТВОЯ ПЕСНЯ / ЗА МГНОВЕНИЕ` | **Исправить** → `ЗА МГНОВЕНЬЕ` |
| `<title>` в `index.html` | «Создай свою песню **за минуту**» | Другое слово; при желании унифицировать с hero — отдельно согласовать |
| Остальной UI (`index.html`, FAQ, модалки) | «мгновение» **не найдено** | — |
| `agent-tools/` (черновики обзора) | упоминания в аналитике | не продукт, не трогать |

- [ ] `#mainHero` `h1`: `МГНОВЕНИЕ` → `МГНОВЕНЬЕ`
- [ ] После правки — пройтись `rg -i мгновен` по проекту (кроме `agent-tools/`)

**Файл:** `index.html` — hero-блок студии

---

### Фон и палитра сайта *(июль 2026, запрос владельца)*

Тёмная zinc+жёлтый тема приелась. Смена возможна без бэкенда — `index.html` (`body`, `.hero-glow`, `.glass-panel`, акцент).

- [ ] Владелец приносит референс / палитру
- [ ] Уровень 1–2: фон + CSS-переменные (см. `SONGFORGE-КОНТЕКСТ.txt` §13c)
- [ ] Проверка контраста: модалки, студия, мобила

### Проблема 6: «Скачать» открывает новую страницу (замечено 03.07.2026)

**Сейчас:** `downloadAudio` ставит ссылку на CDN Suno + `target=_blank` — браузер игнорирует `download` для чужого домена и открывает MP3 в новой вкладке.

**Исправление (v2.4.3):**
- [x] `GET /api/audio/download/{production_id}/{variant}` — прокси с `Content-Disposition: attachment`
- [x] Фронт: `fetch` → `blob` → сохранение на устройство (без новой вкладки)
- [x] Фонотека и модалка результата — через `downloadTrack(generationId, variant)`

### Проблема 5: факты при генерации плохо читаются (замечено 03.07.2026)

**Сейчас:** внизу экрана ожидания (`#funFact`) крутятся интересные факты — цвет **серый** (`text-zinc-500`), на тёмном фоне читается с трудом.

**Где:** `index.html` — блок `#generatingBlock`, элемент:
```html
<div id="funFact" class="... text-xs text-zinc-500 ...">
```

**Нужно (следующая правка UI):**
- [ ] Цвет текста фактов — **белый** (`text-white` или `text-zinc-100`)
- [ ] Шрифт и размер **не менять** — оставить `text-xs`, `leading-relaxed`, отступы и фон блока как есть
- [ ] Только цвет, без правок логики ротации `funFacts`

### Проблема 1: мало фактов, одни и те же по кругу

**Сейчас (v2.1.0):** в `index.html` массив `funFacts` — всего **8 фраз**, случайный выбор каждые 7 сек. Быстро повторяются.

**Нужно:**
- [ ] Расширить библиотеку до **30–50+** фактов (музыка, AI, Suno, жанры, история песен)
- [ ] Не показывать один и тот же факт дважды подряд
- [ ] Опционально: факты по контексту (жанр/настроение выбранного трека)
- [ ] Ротация статусов `genMsgs` — тоже обогатить, сейчас 6 шагов с дублями

### Проблема 2: «Готово!» слишком рано

**Сейчас:** при ~**85–95%** прогресса UI уже пишет «Готово!» / «Секунды до результата…», хотя Suno ещё генерирует **1–3 минуты**.

**Причина в коде (`index.html`):**
- Прогресс искусственно капится на **95%** (`elapsed/240000*95`), пока идёт polling
- Сообщения переключаются по формуле `floor(pct/17)` — последний шаг `genMsgs[5]` = «Готово!» наступает при pct ≥ 85
- Реальный ответ API приходит позже (`pollMusicStatus`, до 48×5 сек)

**Нужно:**
- [x] «Готово!» и **100%** — только при `hideGenerating({success:true})` после success API *(v2.1.5)*
- [x] До успеха — кап прогресса 88%, без «Готово!» в таймере *(v2.1.5)*
- [x] Polling 3 сек вместо 5 *(v2.1.5)*
- [x] Факты 30+ без повтора подряд *(v2.1.5)*
- [ ] Полная привязка % к `progress_hint` с бэкенда

**Файлы:** `index.html` — `funFacts`, `genMsgs`, `showGenerating`, `setGeneratingStatus`, `pollMusicStatus`, `hideGenerating`.

---

## История — полный трек без оплаты (задел на будущее)

**Симптом (замечено июль 2026):** во вкладке **Фонотека → История** кнопки ▶ A / ▶ B дают слушать **весь трек**, хотя в UI написано «Превью 30 сек • не куплено».

**Почему так:**
- В модалке результата (`toggleVariantPreview`) есть обрезка на **30 сек** (`currentTime >= 30` → pause).
- В истории (`renderHistory` → `hist-preview-btn`) вызывается `playAudio(url)` **без лимита** — играет полный `music_url_a/b` с CDN Suno/APIPass.
- API `GET /api/history` отдаёт клиенту **полные URL** — даже с лимитом на фронте ссылку можно скопировать из Network и слушать/скачать напрямую.

**Что сделать (по приоритету):**

### Быстрый фикс (только UI) — сделано v2.1.5
- [x] История: `playDemoAudio` — лимит 30 сек (как в модалке)
- [x] Плеер внизу — превью обрезается на 30 сек

### Надёжный фикс (бэкенд) — частично v2.1.5
- [x] `/api/history` — без полных URL, только `has_preview_a/b`
- [x] `GET /api/history/{id}/preview?variant=a|b` — URL по запросу с проверкой сессии
- [ ] Прокси/ffmpeg trim — полные URL всё ещё в preview endpoint (следующий этап)
- [ ] Полные URL — только после покупки (`/api/library`, purchase response)
- [ ] Опционально: signed URL с коротким TTL для превью

### Данные
- Сейчас в `generations` хранятся только полные `music_url_a/b` (до ~4 мин)
- В `showcase_tracks` уже заложены `preview_url_a/b` — можно переиспользовать идею для истории

**Файлы:** `index.html` — `renderHistory`, `playAudio`, `toggleVariantPreview`; `cabinet_service.py` — `list_history`; `app.py` — новый preview endpoint.

**Пока не менять** — зафиксировано для следующей правки монетизации.

---

## Исправлено: AI-продюсер не генерировал стихи (июль 2026)

**Симптом:** вместо поэтического текста Suno пел описание пользователя дословно.

**Причина:** `AiPromptComposer` просил ЯндексGPT вернуть title + lyrics + style в одном JSON — модель часто копировала идею; при ошибке срабатывал fallback с вставкой идеи в куплет.

**Исправление:**
- Текст песни теперь генерируется **отдельным** вызовом `generate_lyrics()` с промптом «поэт-песенник»
- Composer отвечает только за style/title/параметры
- Проверка `lyrics_look_lazy()` + повторная генерация при «ленивом» ответе
- Fallback без дословной вставки описания

**Файлы:** `prompt_builder.py`, `ai_prompt_composer.py`, `utils/text.py`

---

## Хранение купленных треков — перед публичным запуском

> Зафиксировано: июль 2026, после аудита диска VPS (`df -h`).
> Сейчас: play/download купленных треков — **прямые URL CDN Suno** (ссылки могут протухать).
> Цель: **своя копия каждого купленного трека** + запас по диску.

### Аудит сервера (195.19.20.245, 03.07.2026)

| Параметр | Значение |
|----------|----------|
| Диск `/` всего | 8,6 ГБ |
| Занято | 3,5 ГБ |
| **Свободно** | **5,1 ГБ** |
| RAM (оценка) | ~1 ГБ |

Ориентир: 1 вариант MP3 ≈ 4 МБ, 1 покупка (A+B) ≈ 8 МБ.  
**Безопасный лимит под музыку на текущем VPS: ~3 ГБ** (~350–400 вариантов, ~175–200 покупок).  
Для тысяч треков — **увеличить диск до 40–80 ГБ** или **Object Storage (S3)**.

### Задачи (обязательно перед массовым запуском)

**Backend — копирование при покупке**
- [ ] При `purchase_generation` / prepaid: скачать `music_url_a` и `music_url_b` с CDN на диск
- [ ] Папка: `storage/tracks/{generation_id}/a.mp3`, `b.mp3` (или UUID)
- [ ] В `user_library` / `generations`: поле `local_path` (или отдельная таблица `track_files`)
- [ ] Play и download купленных — с **локального файла** или signed URL, fallback на CDN если копии нет
- [ ] Не копировать непокупленные генерации (только после оплаты ноты)

**Лимиты и мониторинг**
- [ ] Константа `STORAGE_MUSIC_BUDGET_GB` (сейчас рекомендация: 3 ГБ из 5 свободных)
- [ ] Эндпоинт `/api/admin/storage` или скрипт `scripts/storage_report.py`: занято, свободно, число файлов
- [ ] Предупреждение в лог / админке при **свободном диске < 1,5 ГБ** или **музыка > бюджета**
- [ ] Cron или проверка при покупке: отказ с понятным сообщением, если места нет (редкий кейс на старте)

**Инфраструктура (выбрать до роста)**
- [ ] **Вариант A:** у хостера увеличить диск VPS до 40+ ГБ (проще на старте)
- [ ] **Вариант B:** Object Storage (Timeweb / Selectel / Yandex) — VPS только API, MP3 в бакете
- [ ] Бэкап папки `storage/tracks/` (или бакета) — отдельно от кода

**Юридическое / продуктовое**
- [ ] В оферте/FAQ: купленная песня хранится на платформе; при долгом простое ссылка Suno может устареть — после внедрения копий формулировку обновить на «храним у себя»

### Порядок внедрения (предложение)

1. v2.8 — копия на диск при покупке + отдача с `/uploads` или `/api/audio/local/...`
2. v2.8 — `storage_report` + алерт по `df`
3. Перед рекламой — диск 40 ГБ **или** S3, бэкап

**Файлы (задел):** `cabinet_service.py`, `audio_access_service.py`, `app.py`, `settings.py`, `scripts/`

**Пока не менять код** — только backlog; текущая схема CDN остаётся до этой задачи.

---

## Внешний аудит (июль 2026) — что применить

> Источник: сторонний обзор по `app.py` + `requirements.txt`. Часть выводов **устарела** — проект уже модульный, с polling, auth, деплоем.

### Уже есть (аудит не учёл)

- [x] Модули `backend/services/`, `models.py`, `settings.py`, `database/`
- [x] Долгая генерация: `task_id` + `GET /api/music/status/{task_id}` (не синхронно в эндпоинте)
- [x] Фронтенд `index.html`, auth, фонотека, оплата (stub)
- [x] Лимит trial / guest quota
- [x] Деплой: `scripts/update-now.sh`, `DEPLOY-NOW.txt`
- [x] Документация: `SONGFORGE-КОНТЕКСТ.txt`, `docs/ARCHITECTURE.md`

### Сделано по аудиту (июль 2026)

- [x] `.env.example` — шаблон переменных
- [x] `README.md` — краткое описание и запуск
- [x] Smoke-тесты `tests/test_smoke.py` + `requirements-dev.txt`
- [x] `PORT` из env в корневом `app.py`

### Сейчас / перед трафиком

- [ ] Rate limiting на `/api/create-song`, `/api/music/start` (slowapi или Redis)
- [ ] Расширить тесты: purchase, download с сессией
- [ ] `httpx` вместо `requests` в hot-path — при росте нагрузки

### Позже (не перегружать MVP)

- [ ] Celery + Redis — только при сотнях одновременных генераций
- [ ] GitHub Actions (lint + pytest)
- [ ] Docker / docker-compose
- [ ] Разбить `backend/app.py` на `routes/` — рефакторинг по желанию
- [ ] CONTRIBUTING.md — не нужно (закрытый коммерческий продукт)

---

## Админ-панель *(v2.9.0, июль 2026)*

**URL:** `/admin` — отдельная страница, не ссылкается с публичного UI.

**Безопасность:**
- [x] Доступ только после входа на сайте + запись в `admin_users` (или bootstrap по `ADMIN_BOOTSTRAP_EMAILS`)
- [x] Роли и матрица прав (`PERMISSIONS` в `admin_service.py`)
- [x] `admin_audit_log` — все опасные действия с IP
- [x] Опционально `ADMIN_IP_ALLOWLIST` в `.env`
- [x] Support: лимит ±20 нот за одно действие
- [ ] Rate limit на `/api/admin/*` (следующий этап)
- [ ] Re-auth для super_admin действий (следующий этап)
- [x] dev-topup убран с прода (v2.10.7)

**MVP v2.9.0:**
- [x] Дашборд: пользователи, генерации, зависшие, оплаты
- [x] Список генераций
- [x] Поиск пользователей + ± ноты с причиной
- [x] super_admin: выдать роль другому email
- [x] Журнал audit

**v3.0:**
- [ ] Восстановление зависших генераций (кнопка из UI, обёртка `check_task.py`)
- [ ] Модерация МузПлощадки
- [ ] Webhook-лог GetPlatinum
- [ ] Maintenance mode (частично пересекается с § «Объявления» — баннер техработ)
- [ ] sunoapi credits + алерты по балансу внешних API (см. § анализ 8.07)

**Файлы:** `admin.html`, `backend/services/admin_service.py`, `backend/app.py` (`/api/admin/*`)

---

## Внешний аудит 04.07.2026 (`Аудит_СоздайСвоюПесню.docx`)

> Разбор: что актуально, что устарело, что берём в работу. Полный отчёт — в корне проекта.

### Устарело / неточно в отчёте

- Версия в шапке **2.5.9** — сейчас **2.9.0** (админка, МузПлощадка API, редизайн колонок)
- «Исследовать» = заглушка — **нет**: `/api/explore`, лайки, комментарии, публикация из фонотеки
- «1 пробная без регистрации» — **нет**: пробная только **после входа** (v2.5.4+)
- Часть security уже закрыта в v2.4.0 (утечки URL, dev-topup на проде, trial farming) — аудит не учёл

### Блокеры — согласны, актуально

- [ ] Email: `dev_code` в API пока `SMTP_HOST` пустой (1.2.1)
- [ ] `PAYMENT_PROVIDER=stub` + публичный webhook (1.2.2)
- [ ] SQLite без WAL / timeout (1.3.1)
- [ ] HTTPS + `secure` cookie до массового трафика (1.2.7)
- [ ] Реальная оплата до публичного запуска (2.1.1)

### Важно — берём в backlog

- [ ] Атомарное списание: `UPDATE ... WHERE balance >= 1` + rowcount (1.2.4) — частично есть, гонка остаётся
- [ ] Лимит `/api/generate-lyrics` и `/api/produce` (1.2.5) — **записано пользователем**
- [ ] Rate limiting auth + generate (1.3.4)
- [ ] Чекбокс согласия в `#authModal` (4.1.2)
- [ ] ToS: убрать VK/телефон/ЮKassa пока не подключены (4.2.1–4.2.2)
- [ ] Текст consultant.py: «1 пробная», не «2 бесплатные» (2.1.3)
- [ ] Telegram hash перед включением (1.2.3)

### Согласны, не срочно

- CORS сузить, max_length полей, IP-лимит гостя, Tailwind build, SEO/OG, диск/S3, маркетинг (соцдоказательства, цена в ToS, авторские права на лендинг)

### Не противоречит / совпадает с нашими планами

- Хранение MP3, мониторинг диска — `NOTES_NEXT` § хранение
- Шаринг ссылок на треки — заказано отдельно
- USER_GUIDE / честные тексты режимов — `DOCUMENTATION_PLAN.md`
- Админка v2.9 — в отчёте не описана (написан до неё)

---

## Шаринг опубликованных треков *(июль 2026, идея владельца)*

> Цель: как YouTube / Spotify — у опубликованного трека рабочая ссылка для привлечения на сервис.

### Продукт

- После **«Опубликовать»** (фонотека → МузПлощадка) — кнопка **«Поделиться»**
- Модалка: полная ссылка, **Копировать**, опционально «Отправить» (Web Share API на мобиле)
- Получатель открывает ссылку **без входа**: слушает превью/трек, видит название, автор, CTA «Создай свою песню»
- Ссылку можно кинуть в Telegram, VK, форум, сторис (OG-превью для соцсетей — обложка + название)

### Сейчас в коде

- [x] Публикация: `POST /api/library/{id}/publish`, поле `published_at`
- [x] Лента: `GET /api/explore`, лайки, комментарии
- [x] Прослушивание API: `GET /api/explore/{library_id}/listen` (не страница для людей)
- [x] Публичный URL `/t/{library_id}` → редирект на `/?track={id}` *(v2.9.17)*
- [x] `GET /api/explore/{id}/public` — метаданные для deep link
- [x] Кнопка «Поделиться» на МузПлощадке и в фонотеке (опубликованные треки)
- [x] Web Share API на мобиле + копирование ссылки на десктопе
- [x] Открытие ссылки без входа: воспроизведение + переход на МузПлощадку
- [ ] **Премиум OG** — приоритет после беты-минимума *(10.07, владелец)*
- [ ] HTML-страница / Open Graph для превью в Telegram/VK (отдельная `track.html`)
- [ ] Проверка: снятие с публикации → 404 (уже на бэкенде для `/t/` и `/public`)

**Сейчас:** `/t/{id}` → редирект на `/?track=` — соцсети видят только общий og:image (логотип), не обложку трека.

**Цель «премиум»:** в TG/VK/WhatsApp — карточка с обложкой, названием, автором, CTA; на десктопе — лендинг трека (не только SPA).

### Черновик реализации (когда дойдём)

1. **Backend:** `GET /t/{library_id}` — `HTMLResponse` шаблон `track.html` (без редиректа для ботов); `og:image` = cover из S3; `og:audio` опционально
2. **Короткая ссылка (опционально):** slug или hash в `user_library` — не обязательно в v1
3. **Фронт:** `shareTrack(libraryId)` → `https://{SITE_URL}/t/{id}`; `navigator.clipboard` + toast
4. **Аналитика (позже):** счётчик переходов по share для топа/админки
5. **Юридическое:** в оферте — публичная ссылка = трек в общей ленте; автор может снять

### Не путать

- Ссылка на **скачивание MP3** (личная, с сессией) ≠ **публичная страница** для промо
- Пробные 30 сек в истории ≠ шаринг полного опубликованного трека (уточнить политику превью на публичной странице)

**Файлы (задел):** `cabinet_service.py`, `app.py`, `index.html` (фонотека, explore), новый `track.html` или роут в `app.py`

---

## Видео по треку — sunoapi.org MP4 *(10 июля 2026, идея владельца)*

> Цель: «клип» по готовой песне — сильное отличие от конкурентов. Источник: **sunoapi.org** (уже есть `SunoApiOrgClient` для аудио).

### API (docs.sunoapi.org)

| Метод | Endpoint | Назначение |
|-------|----------|------------|
| POST | `/api/v1/mp4/generate` | Создать MP4 по `taskId` + `audioId` + `callBackUrl` |
| GET | `/api/v1/mp4/record-info?taskId=` | Статус + `videoUrl` |
| Callback | webhook | `video_url` готового файла |

Параметры брендинга: `author` (до 50 симв.), `domainName` (водяной знак, напр. `sozdaipesnu.ru`).

Ограничения: MP4 хранится **15 дней** на стороне API → нужен **S3** у нас после готовности.

**Кредиты:** в API есть код `429 Insufficient credits` — «бесплатно» уточнить в ЛК sunoapi.org (возможно 0 кредитов за MP4 или отдельный тариф). Перед внедрением — тестовый запрос + `GET /generate/credit`.

### Черновик продукта

- Кнопка в фонотеке: **«Создать видео»** (после покупки полного трека или для опубликованных)
- Цена для пользователя: 0–N нот (зависит от себестоимости API)
- UI: прогресс → превью MP4 → скачать / поделиться (связка с § шаринг OG)
- Worker: очередь как у Suno audio; webhook `/api/webhooks/sunoapi` расширить под mp4

### Черновик техники

1. `sunoapi_org_client.py` — `create_music_video()`, `get_mp4_status()`
2. БД: поле `video_url` / `video_status` в `user_library` или отдельная таблица
3. Сохранить `provider_task_id` + `audio_id` при генерации музыки (уже частично есть) — нужны для MP4
4. Админ: виджет credits + лог ошибок mp4

### Не в v1

- ApiPass — проверить, есть ли аналог MP4 (пока только sunoapi.org в доке)
- Автогенерация видео при публикации на МузПлощадку

**Приоритет:** после бета-минимума (B/C/D), вместе или сразу после премиум-шаринга (OG).

---

## Левый сайдбар — замечания владельца *(5 июля 2026, после v2.9.2)*

**Контекст:** левая колонка (афиша, советы, топ-3) должна визуально балансировать правую (плеер + инструменты + помощник).

### Сделано (v2.9.3 UI)

- [x] «Сообщения» → **«Афиша»** (иконка газеты)
- [x] Топ-3 чарта: крупные карточки (`sidebarChartTrackHtml`) — обложка 68px, жирное название, лайки, кнопка play
- [x] Колонки `lg:items-stretch` + `h-full` у левой/правой панели; блок чарта `flex-1` — тянется по высоте

### Ротация «Советы студии» — вариант B *(согласовано 5.07)*

- [x] Пул **16** советов, показ **4** одновременно, сдвиг каждые **20 сек** + fade (`studioTipsPool`, `startStudioTipsRotation`) *(v2.9.4)*
- [ ] Позже из админки (вариант D) — связать с блоком «Афиша»

### Топ чарта — обложки крупнее *(5.07)*

- [x] v2.9.3 — горизонтальные карточки с thumb 68px
- [x] v2.9.4 — **обложка на всю ширину** карточки (~124px высота), ранг на cover, play при hover

---

## Ошибка генерации ApiPass / Suno — UNKNOWN_ERR_2129 *(июль 2026)*

### Разбор реального запроса (Тюмень / stadium rap-rock, июль 2026)

**Симптом:** дважды подряд `UNKNOWN_ERR_2129`, в журнале ApiPass «неуспешно».

**Проблемы в теле запроса (не «случайный сбой»):**

| Поле | Было | Лимит Suno §1.3 | Проблема |
|------|------|-----------------|----------|
| `title` | `[Crowd noise, stadium ambience]` | короткое имя | AI взял **первую ремарку из текста**, не название песни |
| `style` | ~800+ символов, дубли | **≤ 200** | `enforce_style` + custom + hip-hop теги раздули строку |
| `negativeTags` | `distortion, screaming, unwanted noise` | — | **Конфликт** со stadium / distorted guitar / crowd |

**Исправление (v2.9.6):** `backend/utils/suno_payload.py` — `compact_suno_style`, `sanitize_suno_title`, `sanitize_negative_tags`; финальная проверка в `apipass_client.create_task`.

---

**Симптом:** после 1–3+ минут ожидания модалка «Не удалось создать песню» с текстом:
`Service temporarily unavailable. Please try again later.（错误码: UNKNOWN_ERR_2129）`

**Что это:** ответ **внешнего** сервиса Suno через **ApiPass** (`recordInfo` → `failMsg`). Не баг SongForge и не связано с версией 2.9.x. Код `UNKNOWN_ERR_2129` — внутренняя/неуточнённая ошибка на стороне генератора (перегрузка, техработы, сбой очереди).

**Что делает сайт при `state=failed`:**
- [x] Пишет `fail_msg` в историю
- [x] Возвращает пробную попытку / ноту (`refund_trial_on_failed`, `refund_if_charged`)

**Пользователю:** подождать 2–5 мин, повторить; проверить баланс нот и **Фонотека → История** (иногда трек всё же появляется позже).

**Улучшение UI (v2.9.6+):** `humanizeGenError` — дружелюбный русский текст вместо EN+中文.

---

## Расширенный режим — план AI-продюсера не совпадает с описанием *(5 июля 2026)*

> **Замечание владельца:** в режиме «Своими словами» детальное EN-описание (rap-rock, stadium, Basta-style vocal…) в блоке решения AI-продюсера превращается в **Pop / Modern Pop, uplifting, BPM 120, vocal auto** — не то, что просил пользователь.

### Корневая причина (разбор кода)

1. **UI** (`buildSongPayload`): даже в `style_mode=custom` на API уходили `genre: "Поп"` и `mood: "uplifting"` (дефолты чипов).
2. **Backend** (`apply_user_to_plan` / `apply_user_to_analysis`): непустой жанр/настроение из запроса **жёстко перезаписывают** результат `AiMusicAnalyst`.
3. **Парсер** (`parse_idea`): раньше смотрел только `idea`, не объединение с `custom_description`.
4. **Отображение** (`renderProducerPlan`): сетка жанр/BPM — это **сжатый MusicAnalysis**, а не дословное описание; при ошибке анализа выглядит как «игнор» текста.
5. **Suno** (см. `Руководство по интеграции апи суно.txt` §1.3): в custom mode главное — поле **`style`** (до ~200 симв. в доке ApiPass; у нас до 950). План в UI ≠ то, что реально уходит в Suno.

### Сделано (v2.9.5 — hotfix)

- [x] В custom mode UI шлёт `genre: ""`, `mood: ""`
- [x] `PromptBuilder.build`: парсит `custom_description`, не применяет списки жанра/настроения
- [x] Текст пользователя **добавляется в `payload.style`** для Suno (приоритет описания)
- [x] Расширены ключевые слова: rap-rock, stadium, anthem, triumphant, patriotic

### Дальше — гибкость и контроль (v2.10+)

**Продукт / UI**

- [ ] В режиме «Своими словами» в плане показывать блок **«Ваше описание»** (первые 2–3 строки) + «AI уточнил»
- [ ] Редактируемые поля плана перед «Создать песню» (жанр, BPM, вокал, инструменты)
- [ ] Подсказка: «Для stadium/rap-rock опишите вокал, продакшн и референс — как в примере из руководства Suno»
- [ ] Режим **«Передать описание в Suno как есть»** (минимум AI-переинтерпретации)

**Backend / пайплайн**

- [ ] `style_mode=custom` → Analyst с `temperature` ниже + обязательное извлечение subgenre из текста (не Pop fallback)
- [ ] При `locked_fields` из `parse_idea` не вызывать `apply_user_to_*` для этих полей
- [ ] Отдельный путь: custom → `style` = нормализованный `custom_description`, lyrics отдельно (Suno §1.3)
- [ ] Лог: `custom_in` vs `analysis_out` vs `style_sent` — для отладки в админке

**Интеграция Suno / ApiPass**

- [ ] Сверить лимит `style` с ApiPass (200 vs 950) — при необходимости сжимать через GPT **с сохранением** ключевых тегов пользователя
- [ ] `styleWeight` / `weirdness` из описания (stadium live → выше styleWeight, ниже weirdness)
- [ ] Документировать в `DEVELOPER_GUIDE`: какой режим Suno (1.3 custom vocal) используем и когда

**Тесты**

- [ ] Интеграционный тест: EN rap-rock custom_description → plan.genre ≠ Pop, style содержит stadium/rap

**Файлы:** `index.html`, `prompt_builder.py`, `ai_music_analyst.py`, `plan_overrides.py`, `apipass_client.py`, `Руководство по интеграции апи суно.txt`

---

## Правый сайдбар — вдохновение и «похожее на хиты» *(5 июля 2026, план)*

> Запланировать к следующим правкам. Код пока не менять без отдельного заказа.

### Проблемы (замечания владельца)

1. **Жанры** под «Вдохновением» — непонятно, зачем они; визуальный шум.
2. **Вдохновение** — после клика только короткая строка в `#randomIdeaPreview`; хочется **развёрнутое описание на всё место** (вместо блока жанров).
3. **«Похожее на хиты»** — неясно, что это; треки совпадают с чартом (ок), но кнопки **«Похожее»** дают **один и тот же шаблон** для всех треков.

### Разделение ролей (зафиксировать в UI)

| Блок | Слева (чарт) | Справа |
|------|----------------|--------|
| Назначение | **Слушать** топ сообщества | **Вдохновиться** для своей песни |
| Действие | Play по обложке | Вставить бриф в студию |
| Не дублировать | — | Тот же список, но другой CTA и текст |

### План v2.10 — «Вдохновение»

- [ ] По клику **«Вдохновение»**: скрыть `#genrePopularBlock` (жанры), показать `#inspirationExpanded` на его месте
- [ ] В развёрнутой карточке: заголовок-идея, 2–3 строки деталей (настроение, жанр, сюжет), кнопки **«Вставить в студию»** и **«Другая идея»**
- [ ] Жанры: убрать отдельную секцию **или** перенести в развёрнутую карточку тегами (Pop, Rock…) — не отдельным блоком «Жанры»
- [ ] Свернуть обратно: крестик или повторный клик «Вдохновение»

**Файлы:** `index.html` — `#randomIdeaBtn`, `#genrePopularList`, новый `#inspirationExpanded`

### План v2.10 — «Идеи из чарта» (бывш. «Похожее на хиты»)

- [ ] Переименовать заголовок → **«Идеи из чарта»** + подпись: *«Готовый бриф по хиту сообщества — нажмите, чтобы вставить в студию»*
- [ ] Уникальный текст для каждого трека (не один шаблон):
  - шаблоны по `rank % 3`: «в духе {title}», «та же энергия, но про …», «{genre}-трек с настроением как у {author}»
  - подставлять `title`, `author_name`, жанр из API если есть
- [ ] UI: мини-обложка + название + кнопка **«Вдохновиться»** (не «Похожее»)
- [ ] Опционально: превью брифа при hover до клика

**Файлы:** `index.html` — `renderCommunityTop()`, `community-similar-btn` → `buildChartIdeaPrompt(track, index)`

---

## Студия — редизайн колонок (план, июль 2026)

> Идея владельца: левый блок «инструменты» перенести вправо под плеер; слева — контент/вдохновение.

**Сделано (v2.9.1 UI, июль 2026):**
- [x] Лево: объявления, советы студии, топ-3 чарта (`#homeExploreSidebar`)
- [x] Право: плеер + вдохновение, жанры, «похожее на хиты», CTA, помощник
- [x] Убран `#homeTrends` из центра
- [x] Правая колонка видна на мобиле (`order-3`); порядок: студия → лента → инструменты
- [x] `renderHomeExplore` → сайдбар + `renderCommunityTop` из API чарта

### Сейчас

| Колонка | Содержимое |
|---------|------------|
| Лево (`lg:w-72`) | Идея дня, жанры, топ сообщества (статик), CTA нот, помощник |
| Центр | Hero, студия, внизу `#homeTrends` — 4 карточки чарта |
| Право (`lg:w-80`, `hidden lg:block`) | Плеер, совет, купить ноты |

**Риски при переносе:** правая колонка на мобиле **скрыта** — если убрать инструменты слева, на телефоне пропадут «Вдохновение», жанры и чат.

### Целевая схема (desktop)

```
[ ЛЕВО: лента/советы ]   [ ЦЕНТР: студия ]   [ ПРАВО: плеер + инструменты ]
```

**Право (под плеером, один `glass-panel` или секции):**
- Плеер (как сейчас)
- Вдохновение + жанры + топ-3 сообщества (компактно)
- CTA «Купить ноты» (один раз — убрать дубль с левого CTA)
- «Спросить помощника»

**Лево (новое):**
- [ ] Доска объявлений (1–2 карточки: бета, акции — позже из админки)
- [ ] Советы качества (3–4 пункта; полный список остаётся в `studio-quality-tips` в центре)
- [ ] Топ-3 чарта — компактный список/мини-карточки (`renderHomeExplore` slice 0–3, убрать `#homeTrends` снизу центра)

**Центр:** только создание песни + hero; без дубля «Популярные треки» внизу (ссылка «Все →» в левом топ-3).

### Мобильная вёрстка (обязательно)

Порядок сейчас: центр → лево (право скрыто).

- [ ] Блок «инструменты» (вдохновение, жанры, помощник) — показывать на мобиле: либо `order-3` под студией, либо секция «Быстрые действия» в центре
- [ ] Плеер на мобиле — опционально компактная полоска или только при воспроизведении (нижняя навигация уже есть)
- [ ] Лево на мобиле: объявления + топ-3 + советы — `order-2` после студии или свёрнутый accordion

### Технически без поломок

- ID кнопок **не менять**: `randomIdeaBtn`, `genrePopularList`, `openChatBtn` — только перенос DOM
- `initSidebarActions()` — без правок логики
- `renderHomeExplore()` — второй контейнер `#homeExploreSidebar` (top 3) + скрыть `#homeTrends` на create
- `switchToView`: боковины видны только на `create` или всегда — решить (на фонотеке/профиле колонки могут мешать)

### Этапы внедрения

1. Перенос правого блока + проверка desktop
2. Мобильная раскладка инструментов
3. Новый левый контент (статик)
4. Топ-3 из API в сайдбар
5. Позже: объявления из админки

---

## Студия — единый скролл страницы *(июль 2026)*

**Было:** левая и правая панели `sticky top-24` — при прокрутке центр уезжал, боковины «залипали», сайт казался раздвоенным.

**Сделано:** убран `sticky` у боковых панелей; у ряда колонок `lg:items-start` — все три части прокручиваются вместе с страницей.

**Позже (если понадобится):** мини-плеер внизу экрана на desktop при прокрутке; или наполнить боковые колонки контентом той же высоты, что центр.

**Файл:** `index.html` — левая/правая `glass-panel`, контейнер `flex ... lg:items-start`

---

## Документация — три уровня *(июль 2026, запланировано)*

> План: `DOCUMENTATION_PLAN.md`

| Уровень | Файл | AI-помощник на сайте |
|---------|------|----------------------|
| Пользователь | `docs/public/USER_GUIDE.md` | **Да** — единственный источник |
| Разработчик | `docs/internal/DEVELOPER_GUIDE.md` | Нет |
| Админ | `docs/internal/ADMIN_GUIDE.md` | Нет |

- [ ] Написать полный USER_GUIDE (из `ВИДЕО-ПРЕЗЕНТАЦИЯ.txt`, FAQ `index.html`, §13 контекста)
- [ ] Написать DEVELOPER_GUIDE (деплой, перенос VPS, `.env`)
- [ ] Расширить ADMIN_GUIDE (сценарии поддержки, RUNBOOK)
- [ ] `ConsultantService` — грузить контекст только из `docs/public/`
- [ ] Синхронизировать устаревший FAQ в `consultant.py` с актуальной моделью нот

---

## Админ-витрина — оживление МузПлощадки *(июль 2026)*

**Цель:** seed-лайки и комменты от персон для своих треков (не для реальных пользователей).

**Сделано (v2.9.14):**
- [x] `users.is_persona`, `track_likes.is_seed`, `track_comments.is_seed`
- [x] `ShowcaseAdminService` + `/api/admin/showcase/*`
- [x] Вкладка «Витрина» в `admin.html`
- [x] Персоны не в поиске пользователей, не логинятся
- [x] Тесты `tests/test_showcase_admin.py`

**v2 (v2.9.15):**
- [x] Пакет «Оживить трек» (12 лайков + 3 коммента, шаблоны текстов)
- [x] Выбор персоны в UI комментария (номер из списка)
- [x] Запись в `admin_audit_log` для showcase-действий

**Мобильная навигация (v2.9.15):**
- [x] Нижняя панель: `grid` 5×1fr, без 6-го пункта «Инфо»
- [x] «Инфо и FAQ» в профиле на мобиле

**v2.9.19 — плеер:**
- [x] Плеер: полоса перемотки + таймер (превью — до 30 сек)

**Откат v2.9.21 — ручная бета-оплата убрана** (риски без платёжки):
- [x] Убраны: баннер −50%, реквизиты, админка «Оплаты β», Telegram-кнопки, `BETA_*` в `.env`
- [x] Задеплоено на сервер

## Надёжность сервера и нагрузка *(июль 2026, handoff)*

**Симптом из аудита:** несколько пользователей одновременно создают песни → сайт «лежит» для всех.

**Причины в коде сейчас:**
- Один worker uvicorn (pm2), блокирующие `requests` к ApiPass/ЯндексGPT держат event loop
- SQLite без `WAL` и без timeout — блокировки при параллельных записях
- Нет rate limiting на `/api/create-song`, `/api/music/*`

**План (не смешивать с доменом — но до рекламы обязательно):**
- [ ] **A.** `PRAGMA journal_mode=WAL` + connect timeout в `db.py`
- [ ] **A.** Rate limit (slowapi): generate-*, auth email, create-song — по IP/user
- [ ] **A.** Лимит одновременных активных генераций на сервере (например max 3–5)
- [ ] **B.** `asyncio.to_thread` / httpx для ApiPass polling
- [ ] **B.** uvicorn `--workers 2` (временная подушка; не замена async)
- [ ] **C.** PostgreSQL, Redis-очередь, отдельный worker для долгих задач
- [ ] Бэкап `songforge.db` по cron на сервере

**Плеер v2.9.22:** фикс обрывов — старый трек больше не гасит новый (`bound===currentAudio`), dispose при смене; фонотека через `/api/library/{id}/listen`; Range на прокси для перемотки.

---

## Плеер — визуализация при воспроизведении *(10 июля 2026, владелец)*

> Цель: при play — «живая» картинка, премиум-ощущение, **без** тяжёлого 3D и без лагов на мобиле.

**Сейчас:** обложка статична (`#playerCover`, `#miniPlayerCover`); есть seek + мини-плеер, анимации под звук нет.

### Идеи (лёгкие по весу)

| Вариант | Суть | Нагрузка |
|---------|------|----------|
| **A. Glow + pulse** | Обложка слегка «дышит», кольцо/аура в такт (CSS + `requestAnimationFrame`, амплитуда из AnalyserNode) | Низкая |
| **B. Мини-спектр** | 8–16 полосок под/вокруг обложки (canvas 2D, `AnalyserNode`, fftSize 64) | Низкая–средняя |
| **C. Blur-фон** | Размытая копия обложки на фоне плеера, яркость от bass | Низкая |
| **D. Частицы** | Редкие искры от центра обложки на битах — только desktop или `prefers-reduced-motion: off` | Средняя |

**Не делать в v1:** Three.js, полноэкранный видеофон, синхрон с каждым ударом барабана (дорого по CPU).

### Техника

- Один `AudioContext` + `createMediaElementSource(currentAudio)` при `startAudioPlayback`; отключать при `stopAudio` (избежать утечек).
- Fallback: если Web Audio недоступен (iOS без жеста) — только CSS-pulse по `playing` без привязки к частотам.
- Те же эффекты в **мини-плеере** (компактный спектр или пульс обложки 44×44).
- `prefers-reduced-motion` → статичная обложка.

### UI

- Правый плеер (desktop) + мини-плеер (мобила при прокрутке).
- Опционально: кнопка «Визуализация вкл/выкл» в настройках профиля (позже).

**Файлы:** `index.html` (CSS, canvas-обёртка у `#playerCover`), JS рядом с `playAudio` / `startAudioPlayback`.

**Приоритет:** после бета-минимума; можно совместить с § видео MP4 (разный уровень: live-визуал vs готовый клип).

---

## Логотип, шапка, favicon, «Добавить на экран» *(10 июля 2026, владелец)*

> Цель: в шапке не голый текст «СоздайСвоюПесню», а **цельный знак в стиле сайта** (glass + accent). Плюс иконки для закладок и домашнего экрана телефона — **автоматом**, без ручной настройки у пользователя.

### Сейчас в коде

- **Шапка:** только текст `#headerBrand`, без картинки *(v2.9.30 убрали «сердце+нота» — выбилось из визуала)*
- **Favicon:** inline SVG — одна нота на тёмном квадрате (`index.html`), не совпадает с `assets/logo.svg` (сердце+нота)
- **OG:** `SongForgeLogo.png` — отдельный файл
- **Генерация:** `#genLogo` — иконка `fa-wand-magic-sparkles`, не логотип
- **На диске есть задел:** `assets/logo.svg`, `logo-header.png`, `logo-64/128/256.png`, `scripts/embed_logos.py` — но в шапке не подключено
- **Нет:** `manifest.webmanifest`, `apple-touch-icon`, единого набора 192/512 для PWA

### Варианты (решить с владельцем)

| Вариант | Описание |
|---------|----------|
| **A. Нота** | Только музыкальная нота (как в favicon) — чище, без «сердца» |
| **B. Нота + слово** | Иконка 36px + «СоздайСвоюПесню» коротким wordmark (мобила: только иконка) |
| **C. Обновить знак** | Новый SVG: нота + искра/волна в `--sf-accent`, скругление как у glass-panel |
| **D. Вернуть сердце+ноту** | `assets/logo-header.png` — но подтянуть под темы (burgundy/olive/obsidian) |

Рекомендация: **один мастер-SVG** → экспорт PNG 64, 128, 192, 512, 180 (apple) → один стиль везде.

### Техника (когда делаем)

1. Шапка: `<a href="/">` + img/SVG + опционально короткий текст; `logo-slot` CSS уже есть
2. `<link rel="icon">` + `apple-touch-icon` 180×180 + `manifest.webmanifest` (`name`, `short_name`, `icons`, `theme_color`, `background_color`)
3. Роут в `app.py` для `/manifest.webmanifest` и статики `/assets/icons/*` (или embed как сейчас favicon)
4. `theme_color` = accent текущей темы или нейтральный `#09090b`
5. Обновить `og:image` на тот же бренд (1200×630 карточка — отдельный `og-brand.png`)
6. Экран генерации: тот же знак в `#genLogo` вместо wand (pulse-logo уже есть)
7. Юрстраницы `/legal/*` — тот же favicon

**Файлы:** `assets/logo.svg`, `index.html`, `backend/app.py`, новый `manifest.webmanifest`, опционально `scripts/embed_logos.py` или `scripts/export_icons.py`

**Приоритет:** можно рано (мало риска) — сразу после бета-минимума или параллельно с OG-шарингом (общий визуальный язык).

---

**Платежи (июль 2026):** Продамус отказал (нет домена+HTTPS). **GetPlatinum** — целевой провайдер.
- [ ] Домен `создайсвоюпесню.рф` → A на `195.19.20.245`
- [ ] nginx reverse-proxy + Let's Encrypt; `SITE_URL=https://…`
- [ ] secure cookie, CORS на домен (аудит 1.2.7)
- [ ] `/legal/offer`, `/legal/privacy`, `/legal/terms` — для анкеты GetPlatinum
- [ ] Интеграция GetPlatinum: `payment_service.py`, webhook `/api/payment/webhook/getplatinum`
- [ ] Интеграция GetPlatinum ↔ «Мой налог» (чеки НПД)
- [ ] Промокод / бета-пакет со скидкой для тестировщиков
- [ ] SMTP (`@домен`) — желательно до публичного запуска
- [ ] Telegram Login (HMAC), VK ID OAuth — после домена
- [ ] OG-превью шаринга треков — до рекламы
- [x] Продамус — не используем (отказ зафиксирован)

**v2.9.18:**
- [x] Витрина «Автор» — меняет `published_author_name` **только у трека**, профиль владельца не трогает
- [x] Советы студии: 1 совет за раз, ротация каждые 15 сек (без наложения)

**v2.9.16:**
- [x] Персоны: креативные ники («Викуся», «Мракобес», «Бешеный меломан»…) вместо «Марина» / «Алексей»
- [x] При создании персоны — проверка уникальности `display_name` среди **всех** пользователей (не только персон)

**Дальше:**
- [ ] super_admin: фильтр «только мои / все треки» в UI
- [ ] Шаблоны комментов по жанру
- [ ] Превью обложки в таблице витрины
- [ ] Кнопка «пересоздать персон» / удалить старые с простыми именами (опционально)

**Файлы:** `backend/services/showcase_admin_service.py`, `admin.html`, `backend/database/db.py`

---

## Уникальные ники пользователей *(июль 2026, запрос владельца)*

**Задача:** при регистрации и смене имени в профиле — **ник не должен совпадать** с уже занятым (как на соцсетях: «Викуся» одна на платформе).

**Сейчас:**
- При входе по email `display_name` = часть до `@` (много `ivan`, `mail`…)
- `PATCH /api/profile` — только длина 2–40, **без проверки уникальности**
- Персоны витрины уже не дублируют ники (v2.9.16)

**Технически возможно.** Варианты внедрения:

| Вариант | Плюсы | Минусы |
|---------|-------|--------|
| **A. Жёсткая уникальность** — `UNIQUE` по `LOWER(display_name)` для `is_persona=0`; 409 «Ник занят» | Честно, как в соцсети | Нужна миграция дублей у существующих; при входе — шаг «выберите ник» |
| **B. Уникальность при публикации** — свободно в профиле, проверка только при «Опубликовать» / комменте | Мягкий старт | Дубли в истории/комментах до публикации |
| **C. Два поля** — `username` (уникальный, @ник) + `display_name` (может повторяться) | Гибко | Больше UI и API |
| **D. Авто-суффикс** — `Викуся` → `Викуся_2` | Быстро в коде | Плохой UX, не «как у людей» |

**Рекомендация:** вариант **A** + при первом входе модалка «Как вас называть на площадке?» (2–40 символов, проверка занятости в реальном времени). Старым пользователям с дублями — авто-суффикс `_2` один раз при миграции.

**Сделано (v2.9.17, вариант A):**
- [x] Миграция: дубли `display_name` разведены суффиксами `_2`, `_3`…
- [x] UNIQUE-индекс по `LOWER(display_name)` для реальных пользователей
- [x] Проверка в `profile_service` (409 «Этот ник уже занят»), в т.ч. коллизия с персонами
- [x] Модалка выбора ника после первого входа (`nickname_confirmed`)
- [x] Профиль: «Ник на площадке» + сообщение о занятости
- [x] `GET /api/profile/nickname-available` — проверка перед сохранением

**Позже:**
- [ ] Список запрещённых слов / модерация ников
- [ ] Жанровые шаблоны комментов витрины (сейчас универсальные «человечные»)

**Файлы:** `auth_service.py`, `profile_service.py`, `index.html` (`#profileNameInput`, модалка после входа), `db.py`

---

## Не в scope ближайшей правки (напоминание)

- Реальная отправка email-кодов (SMTP) — отдельная задача
- Telegram Login Widget — отдельная задача
- Оплата нот (ЮKassa / Робокасса) — отдельная задача

---

## Референс

Сервис **«Моя песня»** — ориентир по UX: выкладка песен, рейтинги, комментарии, сообщество вокруг пользовательского контента.