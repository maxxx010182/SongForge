# SongForge — правила для ассистента

Проект: AI-платформа создания песен (СоздайСвоюПесню).
Папка: `C:\Users\makc0\Desktop\SongForge`

## Перед любой работой прочитать

1. `SONGFORGE-КОНТЕКСТ.txt` — **главный handoff**: текущий статус, следующий шаг, тексты UI
2. `docs/NOTES_NEXT.md` — технический backlog (верх файла = актуально; ниже — архив)
3. `COMMANDS.txt` — команды для сервера (копирует владелец)

Не опираться на старые цифры версий в середине длинных файлов — только на блок **«СЕЙЧАС»** в `SONGFORGE-КОНТЕКСТ.txt` и на `backend/app.py`.

## Команды для сервера

Файл `COMMANDS.txt` (латиница). Пользователь копирует оттуда — из чата Cursor копирование на Windows часто не работает.

Инструкции — только `.txt`, без `.bat`-обёрток (пользователь открывает текстовые файлы сам).

## Деплой и версия

После правок кода — push в GitHub, на сервере: `update-now.sh` (см. COMMANDS.txt).

**Версия в коде (истина):** **2.11.20**
- `backend/app.py` (FastAPI + `/api/health`)
- `scripts/deploy-vps.sh` (`EXPECTED_VERSION`)
- `scripts/deploy-local.sh` (`EXPECTED_VERSION`)
- `README.md`

Деплой: `COMMANDS.txt` (корень). Инструкции: `docs/instrukcii/INDEX.txt`. Блок «esli TLS» если raw.githubusercontent падает.

Историю сессий и старые версии **не** править задним числом в архивных разделах — только обновлять блок «СЕЙЧАС».

## Язык

Общаться с пользователем на русском.

## Работа с владельцем — только по шагам (9.07.2026)

Владелец просил: **не сыпать всё скопом** — иначе смешивается и непонятно, что делать сейчас.

**В чате:**
- Один ответ = **один текущий шаг** (1–3 коротких абзаца, без длинных таблиц и списков «ещё 10 задач»).
- Не перескакивать на следующие шаги, пока владелец не сказал «готово» / «сделал» / задал вопрос.
- Backlog и «на потом» — в `SONGFORGE-КОНТЕКСТ.txt` / `NOTES_NEXT.md`, не в каждое сообщение.
- Команды для копирования — в `.txt` (COMMANDS.txt), в чате максимум одна команда или «открой файл X, шаг N».

## Сейчас (синхронизировано 15.07.2026)

- Прод и код: **v2.11.20** (проверено `/api/health`).
- SMTP, email, TG, VK — работают.
- Аудит: webhook GetPlatinum (v2.11.19) и CORS (v2.11.20) — закрыты.
- Rate limiting: фронт есть (4 перегенерации текста + cooldown 10с на создание песни); backend slowapi — backlog.
- **Активная проблема:** после оплаты GetPlatinum тост «Баланс обновлён», но нота не начисляется. Диагностика — `COMMANDS.txt` (блок АКТУАЛЬНОЕ).
